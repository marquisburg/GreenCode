; Generated by MethASM
; x86-64 Assembly Output

bits 64

section .text
; Code section

; First pass: processing 49 declarations
; Declaration 0 type: 5 (AST_INLINE_ASM = 21)
; Declaration 1 type: 3 (AST_INLINE_ASM = 21)
; Declaration 2 type: 3 (AST_INLINE_ASM = 21)
; Declaration 3 type: 3 (AST_INLINE_ASM = 21)
; Declaration 4 type: 3 (AST_INLINE_ASM = 21)
; Declaration 5 type: 3 (AST_INLINE_ASM = 21)
; Declaration 6 type: 3 (AST_INLINE_ASM = 21)
; Declaration 7 type: 3 (AST_INLINE_ASM = 21)
; Declaration 8 type: 3 (AST_INLINE_ASM = 21)
; Declaration 9 type: 3 (AST_INLINE_ASM = 21)
; Declaration 10 type: 3 (AST_INLINE_ASM = 21)
; Declaration 11 type: 3 (AST_INLINE_ASM = 21)
; Declaration 12 type: 3 (AST_INLINE_ASM = 21)
; Declaration 13 type: 3 (AST_INLINE_ASM = 21)
; Declaration 14 type: 3 (AST_INLINE_ASM = 21)
; Declaration 15 type: 3 (AST_INLINE_ASM = 21)
; Declaration 16 type: 3 (AST_INLINE_ASM = 21)
; Declaration 17 type: 3 (AST_INLINE_ASM = 21)
; Declaration 18 type: 3 (AST_INLINE_ASM = 21)
; Declaration 19 type: 3 (AST_INLINE_ASM = 21)
; Declaration 20 type: 3 (AST_INLINE_ASM = 21)
; Declaration 21 type: 3 (AST_INLINE_ASM = 21)
; Declaration 22 type: 3 (AST_INLINE_ASM = 21)
; Declaration 23 type: 3 (AST_INLINE_ASM = 21)
; Declaration 24 type: 3 (AST_INLINE_ASM = 21)
; Declaration 25 type: 3 (AST_INLINE_ASM = 21)
; Declaration 26 type: 3 (AST_INLINE_ASM = 21)
; Declaration 27 type: 3 (AST_INLINE_ASM = 21)
; Declaration 28 type: 3 (AST_INLINE_ASM = 21)
; Declaration 29 type: 3 (AST_INLINE_ASM = 21)
; Declaration 30 type: 3 (AST_INLINE_ASM = 21)
; Declaration 31 type: 3 (AST_INLINE_ASM = 21)
; Declaration 32 type: 3 (AST_INLINE_ASM = 21)
; Declaration 33 type: 3 (AST_INLINE_ASM = 21)
; Declaration 34 type: 3 (AST_INLINE_ASM = 21)
; Declaration 35 type: 3 (AST_INLINE_ASM = 21)
; Declaration 36 type: 3 (AST_INLINE_ASM = 21)
; Declaration 37 type: 3 (AST_INLINE_ASM = 21)
; Declaration 38 type: 3 (AST_INLINE_ASM = 21)
; Declaration 39 type: 3 (AST_INLINE_ASM = 21)
; Declaration 40 type: 3 (AST_INLINE_ASM = 21)
; Declaration 41 type: 3 (AST_INLINE_ASM = 21)
; Declaration 42 type: 3 (AST_INLINE_ASM = 21)
; Declaration 43 type: 4 (AST_INLINE_ASM = 21)
; Declaration 44 type: 4 (AST_INLINE_ASM = 21)
; Declaration 45 type: 4 (AST_INLINE_ASM = 21)
; Declaration 46 type: 4 (AST_INLINE_ASM = 21)
; Declaration 47 type: 4 (AST_INLINE_ASM = 21)
; Declaration 48 type: 4 (AST_INLINE_ASM = 21)

section .text
    ; Struct declaration: Token
    ; Struct Token: size=20, alignment=4

global is_alpha

is_alpha:
    push rbp        ; Save old base pointer
    mov rbp, rsp  ; Set new base pointer
    sub rsp, 224    ; Allocate 224 bytes on stack (aligned)
    ; Registering 1 function parameters
    mov [rbp - 8], rcx  ; Home param 'c'
    ; Parameter 'c' arrived in register rcx
    mov [rbp - 16], r12
    ; Load variable: c
    movsxd rax, dword [rbp - 8]  ; From stack [rbp - 8]
    ; Store to variable: c
    mov r12d, eax       ; To register (int32/uint32)
ir_entry_0:
    cmp r12d, 97
    jl ir_if_next_2
    cmp r12d, 122
    jg ir_if_next_4
    mov rax, 1
    mov [rbp - 40], rax
    cmp qword [rbp - 40], 0
    jnz ir_errdefer_end_6
ir_errdefer_ok_5:
ir_errdefer_end_6:
    mov rax, 1
    jmp Lis_alpha_exit
    jmp ir_if_end_3
ir_if_next_4:
ir_if_end_3:
    jmp ir_if_end_1
ir_if_next_2:
ir_if_end_1:
    cmp r12d, 65
    jl ir_if_next_8
    cmp r12d, 90
    jg ir_if_next_10
    mov rax, 1
    mov [rbp - 64], rax
    cmp qword [rbp - 64], 0
    jnz ir_errdefer_end_12
ir_errdefer_ok_11:
ir_errdefer_end_12:
    mov rax, 1
    jmp Lis_alpha_exit
    jmp ir_if_end_9
ir_if_next_10:
ir_if_end_9:
    jmp ir_if_end_7
ir_if_next_8:
ir_if_end_7:
    cmp r12d, 95
    jne ir_if_next_14
    mov rax, 1
    mov [rbp - 80], rax
    cmp qword [rbp - 80], 0
    jnz ir_errdefer_end_16
ir_errdefer_ok_15:
ir_errdefer_end_16:
    mov rax, 1
    jmp Lis_alpha_exit
    jmp ir_if_end_13
ir_if_next_14:
ir_if_end_13:
    mov rax, 0
    mov [rbp - 88], rax
    cmp qword [rbp - 88], 0
    jnz ir_errdefer_end_18
ir_errdefer_ok_17:
ir_errdefer_end_18:
    mov rax, 0
    jmp Lis_alpha_exit
Lis_alpha_exit:
    mov r12, [rbp - 16]
    ; Function epilogue
    mov rsp, rbp  ; Restore stack pointer
    pop rbp         ; Restore old base pointer
    ret               ; Return to caller

global is_digit

is_digit:
    push rbp        ; Save old base pointer
    mov rbp, rsp  ; Set new base pointer
    sub rsp, 112    ; Allocate 112 bytes on stack (aligned)
    ; Registering 1 function parameters
    mov [rbp - 8], rcx  ; Home param 'c'
    ; Parameter 'c' arrived in register rcx
ir_entry_19:
    mov rax, 48
    mov r10, rax
    ; Load variable: c
    movsxd rax, dword [rbp - 8]  ; From stack [rbp - 8]
    cmp rax, r10
    jl ir_if_next_21
    mov rax, 57
    mov r10, rax
    ; Load variable: c
    movsxd rax, dword [rbp - 8]  ; From stack [rbp - 8]
    cmp rax, r10
    jg ir_if_next_23
    mov rax, 1
    mov [rbp - 32], rax
    cmp qword [rbp - 32], 0
    jnz ir_errdefer_end_25
ir_errdefer_ok_24:
ir_errdefer_end_25:
    mov rax, 1
    jmp Lis_digit_exit
    jmp ir_if_end_22
ir_if_next_23:
ir_if_end_22:
    jmp ir_if_end_20
ir_if_next_21:
ir_if_end_20:
    mov rax, 0
    mov [rbp - 40], rax
    cmp qword [rbp - 40], 0
    jnz ir_errdefer_end_27
ir_errdefer_ok_26:
ir_errdefer_end_27:
    mov rax, 0
    jmp Lis_digit_exit
Lis_digit_exit:
    ; Function epilogue
    mov rsp, rbp  ; Restore stack pointer
    pop rbp         ; Restore old base pointer
    ret               ; Return to caller

global is_alnum

is_alnum:
    push rbp        ; Save old base pointer
    mov rbp, rsp  ; Set new base pointer
    sub rsp, 224    ; Allocate 224 bytes on stack (aligned)
    ; Registering 1 function parameters
    mov [rbp - 8], rcx  ; Home param 'c'
    ; Parameter 'c' arrived in register rcx
    mov [rbp - 16], r12
    mov [rbp - 24], r13
    ; Spill GPRs so GC sees register-held roots
    push rax
    push rbx
    push rcx
    push rdx
    push rsi
    push rdi
    push r8
    push r9
    push r10
    push r11
    push r12
    push r13
    push r14
    push r15
    ; Spill XMM registers for conservative root scan
    sub rsp, 256
    movdqu [rsp + 0], xmm0
    movdqu [rsp + 16], xmm1
    movdqu [rsp + 32], xmm2
    movdqu [rsp + 48], xmm3
    movdqu [rsp + 64], xmm4
    movdqu [rsp + 80], xmm5
    movdqu [rsp + 96], xmm6
    movdqu [rsp + 112], xmm7
    movdqu [rsp + 128], xmm8
    movdqu [rsp + 144], xmm9
    movdqu [rsp + 160], xmm10
    movdqu [rsp + 176], xmm11
    movdqu [rsp + 192], xmm12
    movdqu [rsp + 208], xmm13
    movdqu [rsp + 224], xmm14
    movdqu [rsp + 240], xmm15
    sub rsp, 32
    mov rcx, rsp
    extern gc_safepoint
    call gc_safepoint
    add rsp, 32
    movdqu xmm0, [rsp + 0]
    movdqu xmm1, [rsp + 16]
    movdqu xmm2, [rsp + 32]
    movdqu xmm3, [rsp + 48]
    movdqu xmm4, [rsp + 64]
    movdqu xmm5, [rsp + 80]
    movdqu xmm6, [rsp + 96]
    movdqu xmm7, [rsp + 112]
    movdqu xmm8, [rsp + 128]
    movdqu xmm9, [rsp + 144]
    movdqu xmm10, [rsp + 160]
    movdqu xmm11, [rsp + 176]
    movdqu xmm12, [rsp + 192]
    movdqu xmm13, [rsp + 208]
    movdqu xmm14, [rsp + 224]
    movdqu xmm15, [rsp + 240]
    add rsp, 256
    pop r15
    pop r14
    pop r13
    pop r12
    pop r11
    pop r10
    pop r9
    pop r8
    pop rdi
    pop rsi
    pop rdx
    pop rcx
    pop rbx
    pop rax
ir_entry_28:
    ; IR call: is_alpha (1 args)
    ; Load variable: c
    movsxd rax, dword [rbp - 8]  ; From stack [rbp - 8]
    mov rcx, rax
    call is_alpha
    ; Integer/pointer return value in rax
    ; 32-bit return value already in eax
    mov [rbp - 32], rax
    mov rax, [rbp - 32]
    ; Store to variable: a
    mov r12d, eax       ; To register (int32/uint32)
    ; IR call: is_digit (1 args)
    ; Load variable: c
    movsxd rax, dword [rbp - 8]  ; From stack [rbp - 8]
    mov rcx, rax
    call is_digit
    ; Integer/pointer return value in rax
    ; 32-bit return value already in eax
    mov [rbp - 40], rax
    mov rax, [rbp - 40]
    ; Store to variable: d
    mov r13d, eax       ; To register (int32/uint32)
    cmp r12d, 0
    je ir_if_next_30
    mov rax, 1
    mov [rbp - 56], rax
    cmp qword [rbp - 56], 0
    jnz ir_errdefer_end_32
ir_errdefer_ok_31:
ir_errdefer_end_32:
    mov rax, 1
    jmp Lis_alnum_exit
    jmp ir_if_end_29
ir_if_next_30:
ir_if_end_29:
    cmp r13d, 0
    je ir_if_next_34
    mov rax, 1
    mov [rbp - 72], rax
    cmp qword [rbp - 72], 0
    jnz ir_errdefer_end_36
ir_errdefer_ok_35:
ir_errdefer_end_36:
    mov rax, 1
    jmp Lis_alnum_exit
    jmp ir_if_end_33
ir_if_next_34:
ir_if_end_33:
    mov rax, 0
    mov [rbp - 80], rax
    cmp qword [rbp - 80], 0
    jnz ir_errdefer_end_38
ir_errdefer_ok_37:
ir_errdefer_end_38:
    mov rax, 0
    jmp Lis_alnum_exit
Lis_alnum_exit:
    mov r13, [rbp - 24]
    mov r12, [rbp - 16]
    ; Function epilogue
    mov rsp, rbp  ; Restore stack pointer
    pop rbp         ; Restore old base pointer
    ret               ; Return to caller

global str_eq

str_eq:
    push rbp        ; Save old base pointer
    mov rbp, rsp  ; Set new base pointer
    sub rsp, 560    ; Allocate 560 bytes on stack (aligned)
    ; Registering 4 function parameters
    mov [rbp - 8], rcx  ; Home param 'source'
    ; Parameter 'source' arrived in register rcx
    mov [rbp - 16], rdx  ; Home param 'start'
    ; Parameter 'start' arrived in register rdx
    mov [rbp - 24], r8  ; Home param 'len'
    ; Parameter 'len' arrived in register r8
    mov [rbp - 32], r9  ; Home param 'keyword'
    ; Parameter 'keyword' arrived in register r9
    mov [rbp - 40], r12
    mov [rbp - 48], r13
    mov [rbp - 56], r14
    mov [rbp - 64], r15
    ; Load variable: keyword
    mov rax, qword [rbp - 32]  ; From stack [rbp - 32]
    ; Store to variable: keyword
    mov r13, rax       ; To register
ir_entry_39:
    mov rax, 0
    ; Store to variable: i
    mov r12d, eax       ; To register (int32/uint32)
ir_while_40:
    ; Load variable: len
    movsxd rax, dword [rbp - 24]  ; From stack [rbp - 24]
    mov r10, rax
    ; Load variable: i
    movsxd rax, r12d      ; From register (signed int32)
    cmp rax, r10
    jge ir_while_end_41
    ; Load variable: i
    movsxd rax, r12d      ; From register (signed int32)
    mov r10, rax
    ; Load variable: start
    movsxd rax, dword [rbp - 16]  ; From stack [rbp - 16]
    add rax, r10
    mov [rbp - 80], rax
    cmp qword [rbp - 8], 0
    jnz ir_nonnull_43
ir_trap_null_42:
    extern puts
    ; IR call: puts (1 args)
    sub rsp, 32
    ; String literal (37 bytes)
    lea rax, [rel Lstr_struct1]  ; Load string struct address
    mov rcx, rax
    call puts
    add rsp, 32
    ; Unknown return type - assuming integer
    extern exit
    ; IR call: exit (1 args)
    sub rsp, 32
    mov rax, 1
    mov rcx, rax
    call exit
    add rsp, 32
    ; Unknown return type - assuming integer
ir_nonnull_43:
    mov rax, 1
    mov r10, rax
    mov rax, [rbp - 80]
    imul rax, r10
    mov [rbp - 88], rax
    mov rax, [rbp - 88]
    mov r10, rax
    ; Load variable: source
    mov rax, qword [rbp - 8]  ; From stack [rbp - 8]
    add rax, r10
    mov [rbp - 96], rax
    mov rax, [rbp - 96]
    movzx rax, byte [rax]
    mov [rbp - 104], rax
    mov rax, [rbp - 104]
    ; Store to variable: sc
    mov r15d, eax       ; To register (int32/uint32)
    test r13, r13
    jnz ir_nonnull_45
ir_trap_null_44:
    ; IR call: puts (1 args)
    sub rsp, 32
    ; String literal (37 bytes)
    lea rax, [rel Lstr_struct3]  ; Load string struct address
    mov rcx, rax
    call puts
    add rsp, 32
    ; Unknown return type - assuming integer
    ; IR call: exit (1 args)
    sub rsp, 32
    mov rax, 1
    mov rcx, rax
    call exit
    add rsp, 32
    ; Unknown return type - assuming integer
ir_nonnull_45:
    mov rax, 1
    mov r10, rax
    ; Load variable: i
    movsxd rax, r12d      ; From register (signed int32)
    imul rax, r10
    mov [rbp - 112], rax
    mov rax, [rbp - 112]
    mov r10, rax
    ; Load variable: keyword
    mov rax, r13      ; From register
    add rax, r10
    mov [rbp - 120], rax
    mov rax, [rbp - 120]
    movzx rax, byte [rax]
    mov [rbp - 128], rax
    mov rax, [rbp - 128]
    ; Store to variable: kc
    mov r14d, eax       ; To register (int32/uint32)
    cmp r14d, 0
    jne ir_if_next_47
    mov rax, 0
    mov [rbp - 144], rax
    cmp qword [rbp - 144], 0
    jnz ir_errdefer_end_49
ir_errdefer_ok_48:
ir_errdefer_end_49:
    mov rax, 0
    jmp Lstr_eq_exit
    jmp ir_if_end_46
ir_if_next_47:
ir_if_end_46:
    cmp r15d, r14d
    je ir_if_next_51
    mov rax, 0
    mov [rbp - 160], rax
    cmp qword [rbp - 160], 0
    jnz ir_errdefer_end_53
ir_errdefer_ok_52:
ir_errdefer_end_53:
    mov rax, 0
    jmp Lstr_eq_exit
    jmp ir_if_end_50
ir_if_next_51:
ir_if_end_50:
    mov rax, 1
    mov r10, rax
    ; Load variable: i
    movsxd rax, r12d      ; From register (signed int32)
    add rax, r10
    mov [rbp - 168], rax
    mov rax, [rbp - 168]
    ; Store to variable: i
    mov r12d, eax       ; To register (int32/uint32)
    jmp ir_while_40
ir_while_end_41:
    test r13, r13
    jnz ir_nonnull_57
ir_trap_null_56:
    ; IR call: puts (1 args)
    sub rsp, 32
    ; String literal (37 bytes)
    lea rax, [rel Lstr_struct5]  ; Load string struct address
    mov rcx, rax
    call puts
    add rsp, 32
    ; Unknown return type - assuming integer
    ; IR call: exit (1 args)
    sub rsp, 32
    mov rax, 1
    mov rcx, rax
    call exit
    add rsp, 32
    ; Unknown return type - assuming integer
ir_nonnull_57:
    mov rax, 1
    mov r10, rax
    ; Load variable: i
    movsxd rax, r12d      ; From register (signed int32)
    imul rax, r10
    mov [rbp - 176], rax
    mov rax, [rbp - 176]
    mov r10, rax
    ; Load variable: keyword
    mov rax, r13      ; From register
    add rax, r10
    mov [rbp - 184], rax
    mov rax, [rbp - 184]
    movzx rax, byte [rax]
    mov [rbp - 192], rax
    mov rax, 0
    mov r10, rax
    mov rax, [rbp - 192]
    cmp rax, r10
    jne ir_if_next_55
    mov rax, 1
    mov [rbp - 208], rax
    cmp qword [rbp - 208], 0
    jnz ir_errdefer_end_59
ir_errdefer_ok_58:
ir_errdefer_end_59:
    mov rax, 1
    jmp Lstr_eq_exit
    jmp ir_if_end_54
ir_if_next_55:
ir_if_end_54:
    mov rax, 0
    mov [rbp - 216], rax
    cmp qword [rbp - 216], 0
    jnz ir_errdefer_end_61
ir_errdefer_ok_60:
ir_errdefer_end_61:
    mov rax, 0
    jmp Lstr_eq_exit
Lstr_eq_exit:
    mov r15, [rbp - 64]
    mov r14, [rbp - 56]
    mov r13, [rbp - 48]
    mov r12, [rbp - 40]
    ; Function epilogue
    mov rsp, rbp  ; Restore stack pointer
    pop rbp         ; Restore old base pointer
    ret               ; Return to caller

global keyword_type

keyword_type:
    push rbp        ; Save old base pointer
    mov rbp, rsp  ; Set new base pointer
    sub rsp, 1088    ; Allocate 1088 bytes on stack (aligned)
    ; Registering 3 function parameters
    mov [rbp - 8], rcx  ; Home param 'source'
    ; Parameter 'source' arrived in register rcx
    mov [rbp - 16], rdx  ; Home param 'start'
    ; Parameter 'start' arrived in register rdx
    mov [rbp - 24], r8  ; Home param 'len'
    ; Parameter 'len' arrived in register r8
    mov [rbp - 32], r12
    mov [rbp - 40], r13
    mov [rbp - 48], r14
    ; Spill GPRs so GC sees register-held roots
    push rax
    push rbx
    push rcx
    push rdx
    push rsi
    push rdi
    push r8
    push r9
    push r10
    push r11
    push r12
    push r13
    push r14
    push r15
    ; Spill XMM registers for conservative root scan
    sub rsp, 256
    movdqu [rsp + 0], xmm0
    movdqu [rsp + 16], xmm1
    movdqu [rsp + 32], xmm2
    movdqu [rsp + 48], xmm3
    movdqu [rsp + 64], xmm4
    movdqu [rsp + 80], xmm5
    movdqu [rsp + 96], xmm6
    movdqu [rsp + 112], xmm7
    movdqu [rsp + 128], xmm8
    movdqu [rsp + 144], xmm9
    movdqu [rsp + 160], xmm10
    movdqu [rsp + 176], xmm11
    movdqu [rsp + 192], xmm12
    movdqu [rsp + 208], xmm13
    movdqu [rsp + 224], xmm14
    movdqu [rsp + 240], xmm15
    sub rsp, 32
    mov rcx, rsp
    extern gc_safepoint
    call gc_safepoint
    add rsp, 32
    movdqu xmm0, [rsp + 0]
    movdqu xmm1, [rsp + 16]
    movdqu xmm2, [rsp + 32]
    movdqu xmm3, [rsp + 48]
    movdqu xmm4, [rsp + 64]
    movdqu xmm5, [rsp + 80]
    movdqu xmm6, [rsp + 96]
    movdqu xmm7, [rsp + 112]
    movdqu xmm8, [rsp + 128]
    movdqu xmm9, [rsp + 144]
    movdqu xmm10, [rsp + 160]
    movdqu xmm11, [rsp + 176]
    movdqu xmm12, [rsp + 192]
    movdqu xmm13, [rsp + 208]
    movdqu xmm14, [rsp + 224]
    movdqu xmm15, [rsp + 240]
    add rsp, 256
    pop r15
    pop r14
    pop r13
    pop r12
    pop r11
    pop r10
    pop r9
    pop r8
    pop rdi
    pop rsi
    pop rdx
    pop rcx
    pop rbx
    pop rax
    ; Load variable: len
    movsxd rax, dword [rbp - 24]  ; From stack [rbp - 24]
    ; Store to variable: len
    mov r12d, eax       ; To register (int32/uint32)
    ; Load variable: source
    mov rax, qword [rbp - 8]  ; From stack [rbp - 8]
    ; Store to variable: source
    mov r13, rax       ; To register
    ; Load variable: start
    movsxd rax, dword [rbp - 16]  ; From stack [rbp - 16]
    ; Store to variable: start
    mov r14d, eax       ; To register (int32/uint32)
ir_entry_62:
    cmp r12d, 2
    jne ir_if_next_64
    ; IR call: str_eq (4 args)
    ; Load variable: source
    mov rax, r13      ; From register
    mov rcx, rax
    ; Load variable: start
    movsxd rax, r14d      ; From register (signed int32)
    mov rdx, rax
    ; Load variable: len
    movsxd rax, r12d      ; From register (signed int32)
    mov r8, rax
    ; String literal (2 bytes)
    lea rax, [rel Lstr_struct7]  ; Load string struct address
    mov r9, rax
    call str_eq
    ; Integer/pointer return value in rax
    ; 32-bit return value already in eax
    mov [rbp - 64], rax
    mov rax, 0
    mov r10, rax
    mov rax, [rbp - 64]
    cmp rax, r10
    je ir_if_next_66
    ; Load variable: TOKEN_FN
    movsxd rax, dword [rel TOKEN_FN]  ; From global memory
    mov [rbp - 80], rax
    cmp qword [rbp - 80], 0
    jnz ir_errdefer_end_68
ir_errdefer_ok_67:
ir_errdefer_end_68:
    ; Load variable: TOKEN_FN
    movsxd rax, dword [rel TOKEN_FN]  ; From global memory
    jmp Lkeyword_type_exit
    jmp ir_if_end_65
ir_if_next_66:
ir_if_end_65:
    ; IR call: str_eq (4 args)
    ; Load variable: source
    mov rax, r13      ; From register
    mov rcx, rax
    ; Load variable: start
    movsxd rax, r14d      ; From register (signed int32)
    mov rdx, rax
    ; Load variable: len
    movsxd rax, r12d      ; From register (signed int32)
    mov r8, rax
    ; String literal (2 bytes)
    lea rax, [rel Lstr_struct9]  ; Load string struct address
    mov r9, rax
    call str_eq
    ; Integer/pointer return value in rax
    ; 32-bit return value already in eax
    mov [rbp - 88], rax
    mov rax, 0
    mov r10, rax
    mov rax, [rbp - 88]
    cmp rax, r10
    je ir_if_next_70
    ; Load variable: TOKEN_IF
    movsxd rax, dword [rel TOKEN_IF]  ; From global memory
    mov [rbp - 104], rax
    cmp qword [rbp - 104], 0
    jnz ir_errdefer_end_72
ir_errdefer_ok_71:
ir_errdefer_end_72:
    ; Load variable: TOKEN_IF
    movsxd rax, dword [rel TOKEN_IF]  ; From global memory
    jmp Lkeyword_type_exit
    jmp ir_if_end_69
ir_if_next_70:
ir_if_end_69:
    jmp ir_if_end_63
ir_if_next_64:
ir_if_end_63:
    cmp r12d, 3
    jne ir_if_next_74
    ; IR call: str_eq (4 args)
    ; Load variable: source
    mov rax, r13      ; From register
    mov rcx, rax
    ; Load variable: start
    movsxd rax, r14d      ; From register (signed int32)
    mov rdx, rax
    ; Load variable: len
    movsxd rax, r12d      ; From register (signed int32)
    mov r8, rax
    ; String literal (3 bytes)
    lea rax, [rel Lstr_struct11]  ; Load string struct address
    mov r9, rax
    call str_eq
    ; Integer/pointer return value in rax
    ; 32-bit return value already in eax
    mov [rbp - 120], rax
    mov rax, 0
    mov r10, rax
    mov rax, [rbp - 120]
    cmp rax, r10
    je ir_if_next_76
    ; Load variable: TOKEN_LET
    movsxd rax, dword [rel TOKEN_LET]  ; From global memory
    mov [rbp - 136], rax
    cmp qword [rbp - 136], 0
    jnz ir_errdefer_end_78
ir_errdefer_ok_77:
ir_errdefer_end_78:
    ; Load variable: TOKEN_LET
    movsxd rax, dword [rel TOKEN_LET]  ; From global memory
    jmp Lkeyword_type_exit
    jmp ir_if_end_75
ir_if_next_76:
ir_if_end_75:
    ; IR call: str_eq (4 args)
    ; Load variable: source
    mov rax, r13      ; From register
    mov rcx, rax
    ; Load variable: start
    movsxd rax, r14d      ; From register (signed int32)
    mov rdx, rax
    ; Load variable: len
    movsxd rax, r12d      ; From register (signed int32)
    mov r8, rax
    ; String literal (3 bytes)
    lea rax, [rel Lstr_struct13]  ; Load string struct address
    mov r9, rax
    call str_eq
    ; Integer/pointer return value in rax
    ; 32-bit return value already in eax
    mov [rbp - 144], rax
    mov rax, 0
    mov r10, rax
    mov rax, [rbp - 144]
    cmp rax, r10
    je ir_if_next_80
    ; Load variable: TOKEN_INT
    movsxd rax, dword [rel TOKEN_INT]  ; From global memory
    mov [rbp - 160], rax
    cmp qword [rbp - 160], 0
    jnz ir_errdefer_end_82
ir_errdefer_ok_81:
ir_errdefer_end_82:
    ; Load variable: TOKEN_INT
    movsxd rax, dword [rel TOKEN_INT]  ; From global memory
    jmp Lkeyword_type_exit
    jmp ir_if_end_79
ir_if_next_80:
ir_if_end_79:
    ; IR call: str_eq (4 args)
    ; Load variable: source
    mov rax, r13      ; From register
    mov rcx, rax
    ; Load variable: start
    movsxd rax, r14d      ; From register (signed int32)
    mov rdx, rax
    ; Load variable: len
    movsxd rax, r12d      ; From register (signed int32)
    mov r8, rax
    ; String literal (3 bytes)
    lea rax, [rel Lstr_struct15]  ; Load string struct address
    mov r9, rax
    call str_eq
    ; Integer/pointer return value in rax
    ; 32-bit return value already in eax
    mov [rbp - 168], rax
    mov rax, 0
    mov r10, rax
    mov rax, [rbp - 168]
    cmp rax, r10
    je ir_if_next_84
    ; Load variable: TOKEN_FOR
    movsxd rax, dword [rel TOKEN_FOR]  ; From global memory
    mov [rbp - 184], rax
    cmp qword [rbp - 184], 0
    jnz ir_errdefer_end_86
ir_errdefer_ok_85:
ir_errdefer_end_86:
    ; Load variable: TOKEN_FOR
    movsxd rax, dword [rel TOKEN_FOR]  ; From global memory
    jmp Lkeyword_type_exit
    jmp ir_if_end_83
ir_if_next_84:
ir_if_end_83:
    jmp ir_if_end_73
ir_if_next_74:
ir_if_end_73:
    cmp r12d, 4
    jne ir_if_next_88
    ; IR call: str_eq (4 args)
    ; Load variable: source
    mov rax, r13      ; From register
    mov rcx, rax
    ; Load variable: start
    movsxd rax, r14d      ; From register (signed int32)
    mov rdx, rax
    ; Load variable: len
    movsxd rax, r12d      ; From register (signed int32)
    mov r8, rax
    ; String literal (4 bytes)
    lea rax, [rel Lstr_struct17]  ; Load string struct address
    mov r9, rax
    call str_eq
    ; Integer/pointer return value in rax
    ; 32-bit return value already in eax
    mov [rbp - 200], rax
    mov rax, 0
    mov r10, rax
    mov rax, [rbp - 200]
    cmp rax, r10
    je ir_if_next_90
    ; Load variable: TOKEN_ELSE
    movsxd rax, dword [rel TOKEN_ELSE]  ; From global memory
    mov [rbp - 216], rax
    cmp qword [rbp - 216], 0
    jnz ir_errdefer_end_92
ir_errdefer_ok_91:
ir_errdefer_end_92:
    ; Load variable: TOKEN_ELSE
    movsxd rax, dword [rel TOKEN_ELSE]  ; From global memory
    jmp Lkeyword_type_exit
    jmp ir_if_end_89
ir_if_next_90:
ir_if_end_89:
    ; IR call: str_eq (4 args)
    ; Load variable: source
    mov rax, r13      ; From register
    mov rcx, rax
    ; Load variable: start
    movsxd rax, r14d      ; From register (signed int32)
    mov rdx, rax
    ; Load variable: len
    movsxd rax, r12d      ; From register (signed int32)
    mov r8, rax
    ; String literal (4 bytes)
    lea rax, [rel Lstr_struct19]  ; Load string struct address
    mov r9, rax
    call str_eq
    ; Integer/pointer return value in rax
    ; 32-bit return value already in eax
    mov [rbp - 224], rax
    mov rax, 0
    mov r10, rax
    mov rax, [rbp - 224]
    cmp rax, r10
    je ir_if_next_94
    ; Load variable: TOKEN_TRUE
    movsxd rax, dword [rel TOKEN_TRUE]  ; From global memory
    mov [rbp - 240], rax
    cmp qword [rbp - 240], 0
    jnz ir_errdefer_end_96
ir_errdefer_ok_95:
ir_errdefer_end_96:
    ; Load variable: TOKEN_TRUE
    movsxd rax, dword [rel TOKEN_TRUE]  ; From global memory
    jmp Lkeyword_type_exit
    jmp ir_if_end_93
ir_if_next_94:
ir_if_end_93:
    ; IR call: str_eq (4 args)
    ; Load variable: source
    mov rax, r13      ; From register
    mov rcx, rax
    ; Load variable: start
    movsxd rax, r14d      ; From register (signed int32)
    mov rdx, rax
    ; Load variable: len
    movsxd rax, r12d      ; From register (signed int32)
    mov r8, rax
    ; String literal (4 bytes)
    lea rax, [rel Lstr_struct21]  ; Load string struct address
    mov r9, rax
    call str_eq
    ; Integer/pointer return value in rax
    ; 32-bit return value already in eax
    mov [rbp - 248], rax
    mov rax, 0
    mov r10, rax
    mov rax, [rbp - 248]
    cmp rax, r10
    je ir_if_next_98
    ; Load variable: TOKEN_BOOL
    movsxd rax, dword [rel TOKEN_BOOL]  ; From global memory
    mov [rbp - 264], rax
    cmp qword [rbp - 264], 0
    jnz ir_errdefer_end_100
ir_errdefer_ok_99:
ir_errdefer_end_100:
    ; Load variable: TOKEN_BOOL
    movsxd rax, dword [rel TOKEN_BOOL]  ; From global memory
    jmp Lkeyword_type_exit
    jmp ir_if_end_97
ir_if_next_98:
ir_if_end_97:
    jmp ir_if_end_87
ir_if_next_88:
ir_if_end_87:
    cmp r12d, 5
    jne ir_if_next_102
    ; IR call: str_eq (4 args)
    ; Load variable: source
    mov rax, r13      ; From register
    mov rcx, rax
    ; Load variable: start
    movsxd rax, r14d      ; From register (signed int32)
    mov rdx, rax
    ; Load variable: len
    movsxd rax, r12d      ; From register (signed int32)
    mov r8, rax
    ; String literal (5 bytes)
    lea rax, [rel Lstr_struct23]  ; Load string struct address
    mov r9, rax
    call str_eq
    ; Integer/pointer return value in rax
    ; 32-bit return value already in eax
    mov [rbp - 280], rax
    mov rax, 0
    mov r10, rax
    mov rax, [rbp - 280]
    cmp rax, r10
    je ir_if_next_104
    ; Load variable: TOKEN_WHILE
    movsxd rax, dword [rel TOKEN_WHILE]  ; From global memory
    mov [rbp - 296], rax
    cmp qword [rbp - 296], 0
    jnz ir_errdefer_end_106
ir_errdefer_ok_105:
ir_errdefer_end_106:
    ; Load variable: TOKEN_WHILE
    movsxd rax, dword [rel TOKEN_WHILE]  ; From global memory
    jmp Lkeyword_type_exit
    jmp ir_if_end_103
ir_if_next_104:
ir_if_end_103:
    ; IR call: str_eq (4 args)
    ; Load variable: source
    mov rax, r13      ; From register
    mov rcx, rax
    ; Load variable: start
    movsxd rax, r14d      ; From register (signed int32)
    mov rdx, rax
    ; Load variable: len
    movsxd rax, r12d      ; From register (signed int32)
    mov r8, rax
    ; String literal (5 bytes)
    lea rax, [rel Lstr_struct25]  ; Load string struct address
    mov r9, rax
    call str_eq
    ; Integer/pointer return value in rax
    ; 32-bit return value already in eax
    mov [rbp - 304], rax
    mov rax, 0
    mov r10, rax
    mov rax, [rbp - 304]
    cmp rax, r10
    je ir_if_next_108
    ; Load variable: TOKEN_FALSE
    movsxd rax, dword [rel TOKEN_FALSE]  ; From global memory
    mov [rbp - 320], rax
    cmp qword [rbp - 320], 0
    jnz ir_errdefer_end_110
ir_errdefer_ok_109:
ir_errdefer_end_110:
    ; Load variable: TOKEN_FALSE
    movsxd rax, dword [rel TOKEN_FALSE]  ; From global memory
    jmp Lkeyword_type_exit
    jmp ir_if_end_107
ir_if_next_108:
ir_if_end_107:
    ; IR call: str_eq (4 args)
    ; Load variable: source
    mov rax, r13      ; From register
    mov rcx, rax
    ; Load variable: start
    movsxd rax, r14d      ; From register (signed int32)
    mov rdx, rax
    ; Load variable: len
    movsxd rax, r12d      ; From register (signed int32)
    mov r8, rax
    ; String literal (5 bytes)
    lea rax, [rel Lstr_struct27]  ; Load string struct address
    mov r9, rax
    call str_eq
    ; Integer/pointer return value in rax
    ; 32-bit return value already in eax
    mov [rbp - 328], rax
    mov rax, 0
    mov r10, rax
    mov rax, [rbp - 328]
    cmp rax, r10
    je ir_if_next_112
    ; Load variable: TOKEN_FLOAT
    movsxd rax, dword [rel TOKEN_FLOAT]  ; From global memory
    mov [rbp - 344], rax
    cmp qword [rbp - 344], 0
    jnz ir_errdefer_end_114
ir_errdefer_ok_113:
ir_errdefer_end_114:
    ; Load variable: TOKEN_FLOAT
    movsxd rax, dword [rel TOKEN_FLOAT]  ; From global memory
    jmp Lkeyword_type_exit
    jmp ir_if_end_111
ir_if_next_112:
ir_if_end_111:
    jmp ir_if_end_101
ir_if_next_102:
ir_if_end_101:
    cmp r12d, 6
    jne ir_if_next_116
    ; IR call: str_eq (4 args)
    ; Load variable: source
    mov rax, r13      ; From register
    mov rcx, rax
    ; Load variable: start
    movsxd rax, r14d      ; From register (signed int32)
    mov rdx, rax
    ; Load variable: len
    movsxd rax, r12d      ; From register (signed int32)
    mov r8, rax
    ; String literal (6 bytes)
    lea rax, [rel Lstr_struct29]  ; Load string struct address
    mov r9, rax
    call str_eq
    ; Integer/pointer return value in rax
    ; 32-bit return value already in eax
    mov [rbp - 360], rax
    mov rax, 0
    mov r10, rax
    mov rax, [rbp - 360]
    cmp rax, r10
    je ir_if_next_118
    ; Load variable: TOKEN_RETURN
    movsxd rax, dword [rel TOKEN_RETURN]  ; From global memory
    mov [rbp - 376], rax
    cmp qword [rbp - 376], 0
    jnz ir_errdefer_end_120
ir_errdefer_ok_119:
ir_errdefer_end_120:
    ; Load variable: TOKEN_RETURN
    movsxd rax, dword [rel TOKEN_RETURN]  ; From global memory
    jmp Lkeyword_type_exit
    jmp ir_if_end_117
ir_if_next_118:
ir_if_end_117:
    ; IR call: str_eq (4 args)
    ; Load variable: source
    mov rax, r13      ; From register
    mov rcx, rax
    ; Load variable: start
    movsxd rax, r14d      ; From register (signed int32)
    mov rdx, rax
    ; Load variable: len
    movsxd rax, r12d      ; From register (signed int32)
    mov r8, rax
    ; String literal (6 bytes)
    lea rax, [rel Lstr_struct31]  ; Load string struct address
    mov r9, rax
    call str_eq
    ; Integer/pointer return value in rax
    ; 32-bit return value already in eax
    mov [rbp - 384], rax
    mov rax, 0
    mov r10, rax
    mov rax, [rbp - 384]
    cmp rax, r10
    je ir_if_next_122
    ; Load variable: TOKEN_STRING_KW
    movsxd rax, dword [rel TOKEN_STRING_KW]  ; From global memory
    mov [rbp - 400], rax
    cmp qword [rbp - 400], 0
    jnz ir_errdefer_end_124
ir_errdefer_ok_123:
ir_errdefer_end_124:
    ; Load variable: TOKEN_STRING_KW
    movsxd rax, dword [rel TOKEN_STRING_KW]  ; From global memory
    jmp Lkeyword_type_exit
    jmp ir_if_end_121
ir_if_next_122:
ir_if_end_121:
    jmp ir_if_end_115
ir_if_next_116:
ir_if_end_115:
    ; Load variable: TOKEN_IDENT
    movsxd rax, dword [rel TOKEN_IDENT]  ; From global memory
    mov [rbp - 408], rax
    cmp qword [rbp - 408], 0
    jnz ir_errdefer_end_126
ir_errdefer_ok_125:
ir_errdefer_end_126:
    ; Load variable: TOKEN_IDENT
    movsxd rax, dword [rel TOKEN_IDENT]  ; From global memory
    jmp Lkeyword_type_exit
Lkeyword_type_exit:
    mov r14, [rbp - 48]
    mov r13, [rbp - 40]
    mov r12, [rbp - 32]
    ; Function epilogue
    mov rsp, rbp  ; Restore stack pointer
    pop rbp         ; Restore old base pointer
    ret               ; Return to caller

global lex_all

lex_all:
    push rbp        ; Save old base pointer
    mov rbp, rsp  ; Set new base pointer
    mov rax, 9536
    extern ___chkstk_ms
    sub rsp, 32
    call ___chkstk_ms
    add rsp, 32
    sub rsp, rax    ; Allocate 9536 bytes on stack (probed)
    ; Registering 3 function parameters
    mov [rbp - 8], rcx  ; Home param 'source'
    ; Parameter 'source' arrived in register rcx
    mov [rbp - 16], rdx  ; Home param 'tokens'
    ; Parameter 'tokens' arrived in register rdx
    mov [rbp - 24], r8  ; Home param 'max_count'
    ; Parameter 'max_count' arrived in register r8
    mov [rbp - 32], r12
    mov [rbp - 40], r13
    mov [rbp - 48], r14
    mov [rbp - 56], r15
    mov [rbp - 64], rbx
    ; Spill GPRs so GC sees register-held roots
    push rax
    push rbx
    push rcx
    push rdx
    push rsi
    push rdi
    push r8
    push r9
    push r10
    push r11
    push r12
    push r13
    push r14
    push r15
    ; Spill XMM registers for conservative root scan
    sub rsp, 256
    movdqu [rsp + 0], xmm0
    movdqu [rsp + 16], xmm1
    movdqu [rsp + 32], xmm2
    movdqu [rsp + 48], xmm3
    movdqu [rsp + 64], xmm4
    movdqu [rsp + 80], xmm5
    movdqu [rsp + 96], xmm6
    movdqu [rsp + 112], xmm7
    movdqu [rsp + 128], xmm8
    movdqu [rsp + 144], xmm9
    movdqu [rsp + 160], xmm10
    movdqu [rsp + 176], xmm11
    movdqu [rsp + 192], xmm12
    movdqu [rsp + 208], xmm13
    movdqu [rsp + 224], xmm14
    movdqu [rsp + 240], xmm15
    sub rsp, 32
    mov rcx, rsp
    extern gc_safepoint
    call gc_safepoint
    add rsp, 32
    movdqu xmm0, [rsp + 0]
    movdqu xmm1, [rsp + 16]
    movdqu xmm2, [rsp + 32]
    movdqu xmm3, [rsp + 48]
    movdqu xmm4, [rsp + 64]
    movdqu xmm5, [rsp + 80]
    movdqu xmm6, [rsp + 96]
    movdqu xmm7, [rsp + 112]
    movdqu xmm8, [rsp + 128]
    movdqu xmm9, [rsp + 144]
    movdqu xmm10, [rsp + 160]
    movdqu xmm11, [rsp + 176]
    movdqu xmm12, [rsp + 192]
    movdqu xmm13, [rsp + 208]
    movdqu xmm14, [rsp + 224]
    movdqu xmm15, [rsp + 240]
    add rsp, 256
    pop r15
    pop r14
    pop r13
    pop r12
    pop r11
    pop r10
    pop r9
    pop r8
    pop rdi
    pop rsi
    pop rdx
    pop rcx
    pop rbx
    pop rax
    ; Load variable: source
    mov rax, qword [rbp - 8]  ; From stack [rbp - 8]
    ; Store to variable: source
    mov r15, rax       ; To register
ir_entry_127:
    mov rax, 0
    ; Store to variable: pos
    mov r13d, eax       ; To register (int32/uint32)
    mov rax, 1
    ; Store to variable: line
    mov dword [rbp - 68], eax  ; To stack [rbp - 68]
    mov rax, 1
    ; Store to variable: col
    mov r14d, eax       ; To register (int32/uint32)
    mov rax, 0
    ; Store to variable: count
    mov dword [rbp - 72], eax  ; To stack [rbp - 72]
ir_while_128:
    ; Load variable: max_count
    movsxd rax, dword [rbp - 24]  ; From stack [rbp - 24]
    mov r10, rax
    ; Load variable: count
    movsxd rax, dword [rbp - 72]  ; From stack [rbp - 72]
    cmp rax, r10
    jge ir_while_end_129
ir_while_130:
    test r15, r15
    jnz ir_nonnull_133
ir_trap_null_132:
    ; IR call: puts (1 args)
    sub rsp, 32
    ; String literal (37 bytes)
    lea rax, [rel Lstr_struct33]  ; Load string struct address
    mov rcx, rax
    call puts
    add rsp, 32
    ; Unknown return type - assuming integer
    ; IR call: exit (1 args)
    sub rsp, 32
    mov rax, 1
    mov rcx, rax
    call exit
    add rsp, 32
    ; Unknown return type - assuming integer
ir_nonnull_133:
    mov rax, 1
    mov r10, rax
    ; Load variable: pos
    movsxd rax, r13d      ; From register (signed int32)
    imul rax, r10
    mov [rbp - 96], rax
    mov rax, [rbp - 96]
    mov r10, rax
    ; Load variable: source
    mov rax, r15      ; From register
    add rax, r10
    mov [rbp - 104], rax
    mov rax, [rbp - 104]
    movzx rax, byte [rax]
    mov [rbp - 112], rax
    mov rax, 32
    mov r10, rax
    mov rax, [rbp - 112]
    cmp rax, r10
    jne ir_while_end_131
    mov rax, 1
    mov r10, rax
    ; Load variable: pos
    movsxd rax, r13d      ; From register (signed int32)
    add rax, r10
    mov [rbp - 128], rax
    mov rax, [rbp - 128]
    ; Store to variable: pos
    mov r13d, eax       ; To register (int32/uint32)
    mov rax, 1
    mov r10, rax
    ; Load variable: col
    movsxd rax, r14d      ; From register (signed int32)
    add rax, r10
    mov [rbp - 136], rax
    mov rax, [rbp - 136]
    ; Store to variable: col
    mov r14d, eax       ; To register (int32/uint32)
    jmp ir_while_130
ir_while_end_131:
ir_while_134:
    test r15, r15
    jnz ir_nonnull_137
ir_trap_null_136:
    ; IR call: puts (1 args)
    sub rsp, 32
    ; String literal (37 bytes)
    lea rax, [rel Lstr_struct35]  ; Load string struct address
    mov rcx, rax
    call puts
    add rsp, 32
    ; Unknown return type - assuming integer
    ; IR call: exit (1 args)
    sub rsp, 32
    mov rax, 1
    mov rcx, rax
    call exit
    add rsp, 32
    ; Unknown return type - assuming integer
ir_nonnull_137:
    mov rax, 1
    mov r10, rax
    ; Load variable: pos
    movsxd rax, r13d      ; From register (signed int32)
    imul rax, r10
    mov [rbp - 144], rax
    mov rax, [rbp - 144]
    mov r10, rax
    ; Load variable: source
    mov rax, r15      ; From register
    add rax, r10
    mov [rbp - 152], rax
    mov rax, [rbp - 152]
    movzx rax, byte [rax]
    mov [rbp - 160], rax
    mov rax, 9
    mov r10, rax
    mov rax, [rbp - 160]
    cmp rax, r10
    jne ir_while_end_135
    mov rax, 1
    mov r10, rax
    ; Load variable: pos
    movsxd rax, r13d      ; From register (signed int32)
    add rax, r10
    mov [rbp - 176], rax
    mov rax, [rbp - 176]
    ; Store to variable: pos
    mov r13d, eax       ; To register (int32/uint32)
    mov rax, 1
    mov r10, rax
    ; Load variable: col
    movsxd rax, r14d      ; From register (signed int32)
    add rax, r10
    mov [rbp - 184], rax
    mov rax, [rbp - 184]
    ; Store to variable: col
    mov r14d, eax       ; To register (int32/uint32)
    jmp ir_while_134
ir_while_end_135:
ir_while_138:
    test r15, r15
    jnz ir_nonnull_141
ir_trap_null_140:
    ; IR call: puts (1 args)
    sub rsp, 32
    ; String literal (37 bytes)
    lea rax, [rel Lstr_struct37]  ; Load string struct address
    mov rcx, rax
    call puts
    add rsp, 32
    ; Unknown return type - assuming integer
    ; IR call: exit (1 args)
    sub rsp, 32
    mov rax, 1
    mov rcx, rax
    call exit
    add rsp, 32
    ; Unknown return type - assuming integer
ir_nonnull_141:
    mov rax, 1
    mov r10, rax
    ; Load variable: pos
    movsxd rax, r13d      ; From register (signed int32)
    imul rax, r10
    mov [rbp - 192], rax
    mov rax, [rbp - 192]
    mov r10, rax
    ; Load variable: source
    mov rax, r15      ; From register
    add rax, r10
    mov [rbp - 200], rax
    mov rax, [rbp - 200]
    movzx rax, byte [rax]
    mov [rbp - 208], rax
    mov rax, 10
    mov r10, rax
    mov rax, [rbp - 208]
    cmp rax, r10
    jne ir_while_end_139
    mov rax, 1
    mov r10, rax
    ; Load variable: pos
    movsxd rax, r13d      ; From register (signed int32)
    add rax, r10
    mov [rbp - 224], rax
    mov rax, [rbp - 224]
    ; Store to variable: pos
    mov r13d, eax       ; To register (int32/uint32)
    mov rax, 1
    mov r10, rax
    ; Load variable: line
    movsxd rax, dword [rbp - 68]  ; From stack [rbp - 68]
    add rax, r10
    mov [rbp - 232], rax
    mov rax, [rbp - 232]
    ; Store to variable: line
    mov dword [rbp - 68], eax  ; To stack [rbp - 68]
    mov rax, 1
    ; Store to variable: col
    mov r14d, eax       ; To register (int32/uint32)
    jmp ir_while_138
ir_while_end_139:
ir_while_142:
    test r15, r15
    jnz ir_nonnull_145
ir_trap_null_144:
    ; IR call: puts (1 args)
    sub rsp, 32
    ; String literal (37 bytes)
    lea rax, [rel Lstr_struct39]  ; Load string struct address
    mov rcx, rax
    call puts
    add rsp, 32
    ; Unknown return type - assuming integer
    ; IR call: exit (1 args)
    sub rsp, 32
    mov rax, 1
    mov rcx, rax
    call exit
    add rsp, 32
    ; Unknown return type - assuming integer
ir_nonnull_145:
    mov rax, 1
    mov r10, rax
    ; Load variable: pos
    movsxd rax, r13d      ; From register (signed int32)
    imul rax, r10
    mov [rbp - 240], rax
    mov rax, [rbp - 240]
    mov r10, rax
    ; Load variable: source
    mov rax, r15      ; From register
    add rax, r10
    mov [rbp - 248], rax
    mov rax, [rbp - 248]
    movzx rax, byte [rax]
    mov [rbp - 256], rax
    mov rax, 13
    mov r10, rax
    mov rax, [rbp - 256]
    cmp rax, r10
    jne ir_while_end_143
    mov rax, 1
    mov r10, rax
    ; Load variable: pos
    movsxd rax, r13d      ; From register (signed int32)
    add rax, r10
    mov [rbp - 272], rax
    mov rax, [rbp - 272]
    ; Store to variable: pos
    mov r13d, eax       ; To register (int32/uint32)
    mov rax, 1
    mov r10, rax
    ; Load variable: col
    movsxd rax, r14d      ; From register (signed int32)
    add rax, r10
    mov [rbp - 280], rax
    mov rax, [rbp - 280]
    ; Store to variable: col
    mov r14d, eax       ; To register (int32/uint32)
    jmp ir_while_142
ir_while_end_143:
    test r15, r15
    jnz ir_nonnull_149
ir_trap_null_148:
    ; IR call: puts (1 args)
    sub rsp, 32
    ; String literal (37 bytes)
    lea rax, [rel Lstr_struct41]  ; Load string struct address
    mov rcx, rax
    call puts
    add rsp, 32
    ; Unknown return type - assuming integer
    ; IR call: exit (1 args)
    sub rsp, 32
    mov rax, 1
    mov rcx, rax
    call exit
    add rsp, 32
    ; Unknown return type - assuming integer
ir_nonnull_149:
    mov rax, 1
    mov r10, rax
    ; Load variable: pos
    movsxd rax, r13d      ; From register (signed int32)
    imul rax, r10
    mov [rbp - 288], rax
    mov rax, [rbp - 288]
    mov r10, rax
    ; Load variable: source
    mov rax, r15      ; From register
    add rax, r10
    mov [rbp - 296], rax
    mov rax, [rbp - 296]
    movzx rax, byte [rax]
    mov [rbp - 304], rax
    mov rax, 0
    mov r10, rax
    mov rax, [rbp - 304]
    cmp rax, r10
    jne ir_if_next_147
    cmp qword [rbp - 16], 0
    jnz ir_nonnull_151
ir_trap_null_150:
    ; IR call: puts (1 args)
    sub rsp, 32
    ; String literal (37 bytes)
    lea rax, [rel Lstr_struct43]  ; Load string struct address
    mov rcx, rax
    call puts
    add rsp, 32
    ; Unknown return type - assuming integer
    ; IR call: exit (1 args)
    sub rsp, 32
    mov rax, 1
    mov rcx, rax
    call exit
    add rsp, 32
    ; Unknown return type - assuming integer
ir_nonnull_151:
    mov rax, 8
    mov r10, rax
    ; Load variable: count
    movsxd rax, dword [rbp - 72]  ; From stack [rbp - 72]
    imul rax, r10
    mov [rbp - 320], rax
    mov rax, [rbp - 320]
    mov r10, rax
    ; Load variable: tokens
    mov rax, qword [rbp - 16]  ; From stack [rbp - 16]
    add rax, r10
    mov [rbp - 328], rax
    mov rax, 0
    mov r10, rax
    mov rax, [rbp - 328]
    add rax, r10
    mov [rbp - 336], rax
    ; Load variable: TOKEN_EOF
    movsxd rax, dword [rel TOKEN_EOF]  ; From global memory
    mov rcx, rax
    mov rax, [rbp - 336]
    mov dword [rax], ecx
    cmp qword [rbp - 16], 0
    jnz ir_nonnull_153
ir_trap_null_152:
    ; IR call: puts (1 args)
    sub rsp, 32
    ; String literal (37 bytes)
    lea rax, [rel Lstr_struct45]  ; Load string struct address
    mov rcx, rax
    call puts
    add rsp, 32
    ; Unknown return type - assuming integer
    ; IR call: exit (1 args)
    sub rsp, 32
    mov rax, 1
    mov rcx, rax
    call exit
    add rsp, 32
    ; Unknown return type - assuming integer
ir_nonnull_153:
    mov rax, 8
    mov r10, rax
    ; Load variable: count
    movsxd rax, dword [rbp - 72]  ; From stack [rbp - 72]
    imul rax, r10
    mov [rbp - 352], rax
    mov rax, [rbp - 352]
    mov r10, rax
    ; Load variable: tokens
    mov rax, qword [rbp - 16]  ; From stack [rbp - 16]
    add rax, r10
    mov [rbp - 360], rax
    mov rax, 4
    mov r10, rax
    mov rax, [rbp - 360]
    add rax, r10
    mov [rbp - 368], rax
    ; Load variable: pos
    movsxd rax, r13d      ; From register (signed int32)
    mov rcx, rax
    mov rax, [rbp - 368]
    mov dword [rax], ecx
    cmp qword [rbp - 16], 0
    jnz ir_nonnull_155
ir_trap_null_154:
    ; IR call: puts (1 args)
    sub rsp, 32
    ; String literal (37 bytes)
    lea rax, [rel Lstr_struct47]  ; Load string struct address
    mov rcx, rax
    call puts
    add rsp, 32
    ; Unknown return type - assuming integer
    ; IR call: exit (1 args)
    sub rsp, 32
    mov rax, 1
    mov rcx, rax
    call exit
    add rsp, 32
    ; Unknown return type - assuming integer
ir_nonnull_155:
    mov rax, 8
    mov r10, rax
    ; Load variable: count
    movsxd rax, dword [rbp - 72]  ; From stack [rbp - 72]
    imul rax, r10
    mov [rbp - 384], rax
    mov rax, [rbp - 384]
    mov r10, rax
    ; Load variable: tokens
    mov rax, qword [rbp - 16]  ; From stack [rbp - 16]
    add rax, r10
    mov [rbp - 392], rax
    mov rax, 8
    mov r10, rax
    mov rax, [rbp - 392]
    add rax, r10
    mov [rbp - 400], rax
    mov rax, 0
    mov rcx, rax
    mov rax, [rbp - 400]
    mov dword [rax], ecx
    cmp qword [rbp - 16], 0
    jnz ir_nonnull_157
ir_trap_null_156:
    ; IR call: puts (1 args)
    sub rsp, 32
    ; String literal (37 bytes)
    lea rax, [rel Lstr_struct49]  ; Load string struct address
    mov rcx, rax
    call puts
    add rsp, 32
    ; Unknown return type - assuming integer
    ; IR call: exit (1 args)
    sub rsp, 32
    mov rax, 1
    mov rcx, rax
    call exit
    add rsp, 32
    ; Unknown return type - assuming integer
ir_nonnull_157:
    mov rax, 8
    mov r10, rax
    ; Load variable: count
    movsxd rax, dword [rbp - 72]  ; From stack [rbp - 72]
    imul rax, r10
    mov [rbp - 416], rax
    mov rax, [rbp - 416]
    mov r10, rax
    ; Load variable: tokens
    mov rax, qword [rbp - 16]  ; From stack [rbp - 16]
    add rax, r10
    mov [rbp - 424], rax
    mov rax, 12
    mov r10, rax
    mov rax, [rbp - 424]
    add rax, r10
    mov [rbp - 432], rax
    ; Load variable: line
    movsxd rax, dword [rbp - 68]  ; From stack [rbp - 68]
    mov rcx, rax
    mov rax, [rbp - 432]
    mov dword [rax], ecx
    cmp qword [rbp - 16], 0
    jnz ir_nonnull_159
ir_trap_null_158:
    ; IR call: puts (1 args)
    sub rsp, 32
    ; String literal (37 bytes)
    lea rax, [rel Lstr_struct51]  ; Load string struct address
    mov rcx, rax
    call puts
    add rsp, 32
    ; Unknown return type - assuming integer
    ; IR call: exit (1 args)
    sub rsp, 32
    mov rax, 1
    mov rcx, rax
    call exit
    add rsp, 32
    ; Unknown return type - assuming integer
ir_nonnull_159:
    mov rax, 8
    mov r10, rax
    ; Load variable: count
    movsxd rax, dword [rbp - 72]  ; From stack [rbp - 72]
    imul rax, r10
    mov [rbp - 448], rax
    mov rax, [rbp - 448]
    mov r10, rax
    ; Load variable: tokens
    mov rax, qword [rbp - 16]  ; From stack [rbp - 16]
    add rax, r10
    mov [rbp - 456], rax
    mov rax, 16
    mov r10, rax
    mov rax, [rbp - 456]
    add rax, r10
    mov [rbp - 464], rax
    ; Load variable: col
    movsxd rax, r14d      ; From register (signed int32)
    mov rcx, rax
    mov rax, [rbp - 464]
    mov dword [rax], ecx
    mov rax, 1
    mov r10, rax
    ; Load variable: count
    movsxd rax, dword [rbp - 72]  ; From stack [rbp - 72]
    add rax, r10
    mov [rbp - 480], rax
    mov rax, [rbp - 480]
    ; Store to variable: count
    mov dword [rbp - 72], eax  ; To stack [rbp - 72]
    ; Load variable: count
    movsxd rax, dword [rbp - 72]  ; From stack [rbp - 72]
    mov [rbp - 488], rax
    cmp qword [rbp - 488], 0
    jnz ir_errdefer_end_161
ir_errdefer_ok_160:
ir_errdefer_end_161:
    ; Load variable: count
    movsxd rax, dword [rbp - 72]  ; From stack [rbp - 72]
    jmp Llex_all_exit
    jmp ir_if_end_146
ir_if_next_147:
ir_if_end_146:
    test r15, r15
    jnz ir_nonnull_165
ir_trap_null_164:
    ; IR call: puts (1 args)
    sub rsp, 32
    ; String literal (37 bytes)
    lea rax, [rel Lstr_struct53]  ; Load string struct address
    mov rcx, rax
    call puts
    add rsp, 32
    ; Unknown return type - assuming integer
    ; IR call: exit (1 args)
    sub rsp, 32
    mov rax, 1
    mov rcx, rax
    call exit
    add rsp, 32
    ; Unknown return type - assuming integer
ir_nonnull_165:
    mov rax, 1
    mov r10, rax
    ; Load variable: pos
    movsxd rax, r13d      ; From register (signed int32)
    imul rax, r10
    mov [rbp - 496], rax
    mov rax, [rbp - 496]
    mov r10, rax
    ; Load variable: source
    mov rax, r15      ; From register
    add rax, r10
    mov [rbp - 504], rax
    mov rax, [rbp - 504]
    movzx rax, byte [rax]
    mov [rbp - 512], rax
    mov rax, 47
    mov r10, rax
    mov rax, [rbp - 512]
    cmp rax, r10
    jne ir_if_next_163
    mov rax, 1
    mov r10, rax
    ; Load variable: pos
    movsxd rax, r13d      ; From register (signed int32)
    add rax, r10
    mov [rbp - 528], rax
    test r15, r15
    jnz ir_nonnull_169
ir_trap_null_168:
    ; IR call: puts (1 args)
    sub rsp, 32
    ; String literal (37 bytes)
    lea rax, [rel Lstr_struct55]  ; Load string struct address
    mov rcx, rax
    call puts
    add rsp, 32
    ; Unknown return type - assuming integer
    ; IR call: exit (1 args)
    sub rsp, 32
    mov rax, 1
    mov rcx, rax
    call exit
    add rsp, 32
    ; Unknown return type - assuming integer
ir_nonnull_169:
    mov rax, 1
    mov r10, rax
    mov rax, [rbp - 528]
    imul rax, r10
    mov [rbp - 536], rax
    mov rax, [rbp - 536]
    mov r10, rax
    ; Load variable: source
    mov rax, r15      ; From register
    add rax, r10
    mov [rbp - 544], rax
    mov rax, [rbp - 544]
    movzx rax, byte [rax]
    mov [rbp - 552], rax
    mov rax, 47
    mov r10, rax
    mov rax, [rbp - 552]
    cmp rax, r10
    jne ir_if_next_167
    mov rax, 2
    mov r10, rax
    ; Load variable: pos
    movsxd rax, r13d      ; From register (signed int32)
    add rax, r10
    mov [rbp - 568], rax
    mov rax, [rbp - 568]
    ; Store to variable: pos
    mov r13d, eax       ; To register (int32/uint32)
    mov rax, 2
    mov r10, rax
    ; Load variable: col
    movsxd rax, r14d      ; From register (signed int32)
    add rax, r10
    mov [rbp - 576], rax
    mov rax, [rbp - 576]
    ; Store to variable: col
    mov r14d, eax       ; To register (int32/uint32)
ir_while_170:
    test r15, r15
    jnz ir_nonnull_173
ir_trap_null_172:
    ; IR call: puts (1 args)
    sub rsp, 32
    ; String literal (37 bytes)
    lea rax, [rel Lstr_struct57]  ; Load string struct address
    mov rcx, rax
    call puts
    add rsp, 32
    ; Unknown return type - assuming integer
    ; IR call: exit (1 args)
    sub rsp, 32
    mov rax, 1
    mov rcx, rax
    call exit
    add rsp, 32
    ; Unknown return type - assuming integer
ir_nonnull_173:
    mov rax, 1
    mov r10, rax
    ; Load variable: pos
    movsxd rax, r13d      ; From register (signed int32)
    imul rax, r10
    mov [rbp - 584], rax
    mov rax, [rbp - 584]
    mov r10, rax
    ; Load variable: source
    mov rax, r15      ; From register
    add rax, r10
    mov [rbp - 592], rax
    mov rax, [rbp - 592]
    movzx rax, byte [rax]
    mov [rbp - 600], rax
    mov rax, 0
    mov r10, rax
    mov rax, [rbp - 600]
    cmp rax, r10
    je ir_while_end_171
    test r15, r15
    jnz ir_nonnull_177
ir_trap_null_176:
    ; IR call: puts (1 args)
    sub rsp, 32
    ; String literal (37 bytes)
    lea rax, [rel Lstr_struct59]  ; Load string struct address
    mov rcx, rax
    call puts
    add rsp, 32
    ; Unknown return type - assuming integer
    ; IR call: exit (1 args)
    sub rsp, 32
    mov rax, 1
    mov rcx, rax
    call exit
    add rsp, 32
    ; Unknown return type - assuming integer
ir_nonnull_177:
    mov rax, 1
    mov r10, rax
    ; Load variable: pos
    movsxd rax, r13d      ; From register (signed int32)
    imul rax, r10
    mov [rbp - 616], rax
    mov rax, [rbp - 616]
    mov r10, rax
    ; Load variable: source
    mov rax, r15      ; From register
    add rax, r10
    mov [rbp - 624], rax
    mov rax, [rbp - 624]
    movzx rax, byte [rax]
    mov [rbp - 632], rax
    mov rax, 10
    mov r10, rax
    mov rax, [rbp - 632]
    cmp rax, r10
    jne ir_if_next_175
    mov rax, 1
    mov r10, rax
    ; Load variable: pos
    movsxd rax, r13d      ; From register (signed int32)
    add rax, r10
    mov [rbp - 648], rax
    mov rax, [rbp - 648]
    ; Store to variable: pos
    mov r13d, eax       ; To register (int32/uint32)
    mov rax, 1
    mov r10, rax
    ; Load variable: line
    movsxd rax, dword [rbp - 68]  ; From stack [rbp - 68]
    add rax, r10
    mov [rbp - 656], rax
    mov rax, [rbp - 656]
    ; Store to variable: line
    mov dword [rbp - 68], eax  ; To stack [rbp - 68]
    mov rax, 1
    ; Store to variable: col
    mov r14d, eax       ; To register (int32/uint32)
    jmp ir_while_end_171
    jmp ir_if_end_174
ir_if_next_175:
ir_if_end_174:
    mov rax, 1
    mov r10, rax
    ; Load variable: pos
    movsxd rax, r13d      ; From register (signed int32)
    add rax, r10
    mov [rbp - 664], rax
    mov rax, [rbp - 664]
    ; Store to variable: pos
    mov r13d, eax       ; To register (int32/uint32)
    mov rax, 1
    mov r10, rax
    ; Load variable: col
    movsxd rax, r14d      ; From register (signed int32)
    add rax, r10
    mov [rbp - 672], rax
    mov rax, [rbp - 672]
    ; Store to variable: col
    mov r14d, eax       ; To register (int32/uint32)
    jmp ir_while_170
ir_while_end_171:
    jmp ir_while_128
    jmp ir_if_end_166
ir_if_next_167:
ir_if_end_166:
    jmp ir_if_end_162
ir_if_next_163:
ir_if_end_162:
    cmp qword [rbp - 16], 0
    jnz ir_nonnull_179
ir_trap_null_178:
    ; IR call: puts (1 args)
    sub rsp, 32
    ; String literal (37 bytes)
    lea rax, [rel Lstr_struct61]  ; Load string struct address
    mov rcx, rax
    call puts
    add rsp, 32
    ; Unknown return type - assuming integer
    ; IR call: exit (1 args)
    sub rsp, 32
    mov rax, 1
    mov rcx, rax
    call exit
    add rsp, 32
    ; Unknown return type - assuming integer
ir_nonnull_179:
    mov rax, 8
    mov r10, rax
    ; Load variable: count
    movsxd rax, dword [rbp - 72]  ; From stack [rbp - 72]
    imul rax, r10
    mov [rbp - 680], rax
    mov rax, [rbp - 680]
    mov r10, rax
    ; Load variable: tokens
    mov rax, qword [rbp - 16]  ; From stack [rbp - 16]
    add rax, r10
    mov [rbp - 688], rax
    mov rax, [rbp - 688]
    ; Store to variable: tok
    mov r12, rax       ; To register
    test r12, r12
    jnz ir_nonnull_181
ir_trap_null_180:
    ; IR call: puts (1 args)
    sub rsp, 32
    ; String literal (37 bytes)
    lea rax, [rel Lstr_struct63]  ; Load string struct address
    mov rcx, rax
    call puts
    add rsp, 32
    ; Unknown return type - assuming integer
    ; IR call: exit (1 args)
    sub rsp, 32
    mov rax, 1
    mov rcx, rax
    call exit
    add rsp, 32
    ; Unknown return type - assuming integer
ir_nonnull_181:
    mov rax, 12
    mov r10, rax
    ; Load variable: tok
    mov rax, r12      ; From register
    add rax, r10
    mov [rbp - 696], rax
    ; Load variable: line
    movsxd rax, dword [rbp - 68]  ; From stack [rbp - 68]
    mov rcx, rax
    mov rax, [rbp - 696]
    mov dword [rax], ecx
    test r12, r12
    jnz ir_nonnull_183
ir_trap_null_182:
    ; IR call: puts (1 args)
    sub rsp, 32
    ; String literal (37 bytes)
    lea rax, [rel Lstr_struct65]  ; Load string struct address
    mov rcx, rax
    call puts
    add rsp, 32
    ; Unknown return type - assuming integer
    ; IR call: exit (1 args)
    sub rsp, 32
    mov rax, 1
    mov rcx, rax
    call exit
    add rsp, 32
    ; Unknown return type - assuming integer
ir_nonnull_183:
    mov rax, 16
    mov r10, rax
    ; Load variable: tok
    mov rax, r12      ; From register
    add rax, r10
    mov [rbp - 712], rax
    ; Load variable: col
    movsxd rax, r14d      ; From register (signed int32)
    mov rcx, rax
    mov rax, [rbp - 712]
    mov dword [rax], ecx
    test r12, r12
    jnz ir_nonnull_185
ir_trap_null_184:
    ; IR call: puts (1 args)
    sub rsp, 32
    ; String literal (37 bytes)
    lea rax, [rel Lstr_struct67]  ; Load string struct address
    mov rcx, rax
    call puts
    add rsp, 32
    ; Unknown return type - assuming integer
    ; IR call: exit (1 args)
    sub rsp, 32
    mov rax, 1
    mov rcx, rax
    call exit
    add rsp, 32
    ; Unknown return type - assuming integer
ir_nonnull_185:
    mov rax, 4
    mov r10, rax
    ; Load variable: tok
    mov rax, r12      ; From register
    add rax, r10
    mov [rbp - 728], rax
    ; Load variable: pos
    movsxd rax, r13d      ; From register (signed int32)
    mov rcx, rax
    mov rax, [rbp - 728]
    mov dword [rax], ecx
    test r15, r15
    jnz ir_nonnull_187
ir_trap_null_186:
    ; IR call: puts (1 args)
    sub rsp, 32
    ; String literal (37 bytes)
    lea rax, [rel Lstr_struct69]  ; Load string struct address
    mov rcx, rax
    call puts
    add rsp, 32
    ; Unknown return type - assuming integer
    ; IR call: exit (1 args)
    sub rsp, 32
    mov rax, 1
    mov rcx, rax
    call exit
    add rsp, 32
    ; Unknown return type - assuming integer
ir_nonnull_187:
    mov rax, 1
    mov r10, rax
    ; Load variable: pos
    movsxd rax, r13d      ; From register (signed int32)
    imul rax, r10
    mov [rbp - 744], rax
    mov rax, [rbp - 744]
    mov r10, rax
    ; Load variable: source
    mov rax, r15      ; From register
    add rax, r10
    mov [rbp - 752], rax
    mov rax, [rbp - 752]
    movzx rax, byte [rax]
    mov [rbp - 760], rax
    mov rax, [rbp - 760]
    ; Store to variable: c
    mov ebx, eax       ; To register (int32/uint32)
    cmp ebx, 40
    jne ir_if_next_189
    test r12, r12
    jnz ir_nonnull_191
ir_trap_null_190:
    ; IR call: puts (1 args)
    sub rsp, 32
    ; String literal (37 bytes)
    lea rax, [rel Lstr_struct71]  ; Load string struct address
    mov rcx, rax
    call puts
    add rsp, 32
    ; Unknown return type - assuming integer
    ; IR call: exit (1 args)
    sub rsp, 32
    mov rax, 1
    mov rcx, rax
    call exit
    add rsp, 32
    ; Unknown return type - assuming integer
ir_nonnull_191:
    mov rax, 0
    mov r10, rax
    ; Load variable: tok
    mov rax, r12      ; From register
    add rax, r10
    mov [rbp - 776], rax
    ; Load variable: TOKEN_LPAREN
    movsxd rax, dword [rel TOKEN_LPAREN]  ; From global memory
    mov rcx, rax
    mov rax, [rbp - 776]
    mov dword [rax], ecx
    test r12, r12
    jnz ir_nonnull_193
ir_trap_null_192:
    ; IR call: puts (1 args)
    sub rsp, 32
    ; String literal (37 bytes)
    lea rax, [rel Lstr_struct73]  ; Load string struct address
    mov rcx, rax
    call puts
    add rsp, 32
    ; Unknown return type - assuming integer
    ; IR call: exit (1 args)
    sub rsp, 32
    mov rax, 1
    mov rcx, rax
    call exit
    add rsp, 32
    ; Unknown return type - assuming integer
ir_nonnull_193:
    mov rax, 8
    mov r10, rax
    ; Load variable: tok
    mov rax, r12      ; From register
    add rax, r10
    mov [rbp - 792], rax
    mov rax, 1
    mov rcx, rax
    mov rax, [rbp - 792]
    mov dword [rax], ecx
    mov rax, 1
    mov r10, rax
    ; Load variable: pos
    movsxd rax, r13d      ; From register (signed int32)
    add rax, r10
    mov [rbp - 808], rax
    mov rax, [rbp - 808]
    ; Store to variable: pos
    mov r13d, eax       ; To register (int32/uint32)
    mov rax, 1
    mov r10, rax
    ; Load variable: col
    movsxd rax, r14d      ; From register (signed int32)
    add rax, r10
    mov [rbp - 816], rax
    mov rax, [rbp - 816]
    ; Store to variable: col
    mov r14d, eax       ; To register (int32/uint32)
    jmp ir_if_end_188
ir_if_next_189:
    cmp ebx, 41
    jne ir_if_next_194
    test r12, r12
    jnz ir_nonnull_196
ir_trap_null_195:
    ; IR call: puts (1 args)
    sub rsp, 32
    ; String literal (37 bytes)
    lea rax, [rel Lstr_struct75]  ; Load string struct address
    mov rcx, rax
    call puts
    add rsp, 32
    ; Unknown return type - assuming integer
    ; IR call: exit (1 args)
    sub rsp, 32
    mov rax, 1
    mov rcx, rax
    call exit
    add rsp, 32
    ; Unknown return type - assuming integer
ir_nonnull_196:
    mov rax, 0
    mov r10, rax
    ; Load variable: tok
    mov rax, r12      ; From register
    add rax, r10
    mov [rbp - 832], rax
    ; Load variable: TOKEN_RPAREN
    movsxd rax, dword [rel TOKEN_RPAREN]  ; From global memory
    mov rcx, rax
    mov rax, [rbp - 832]
    mov dword [rax], ecx
    test r12, r12
    jnz ir_nonnull_198
ir_trap_null_197:
    ; IR call: puts (1 args)
    sub rsp, 32
    ; String literal (37 bytes)
    lea rax, [rel Lstr_struct77]  ; Load string struct address
    mov rcx, rax
    call puts
    add rsp, 32
    ; Unknown return type - assuming integer
    ; IR call: exit (1 args)
    sub rsp, 32
    mov rax, 1
    mov rcx, rax
    call exit
    add rsp, 32
    ; Unknown return type - assuming integer
ir_nonnull_198:
    mov rax, 8
    mov r10, rax
    ; Load variable: tok
    mov rax, r12      ; From register
    add rax, r10
    mov [rbp - 848], rax
    mov rax, 1
    mov rcx, rax
    mov rax, [rbp - 848]
    mov dword [rax], ecx
    mov rax, 1
    mov r10, rax
    ; Load variable: pos
    movsxd rax, r13d      ; From register (signed int32)
    add rax, r10
    mov [rbp - 864], rax
    mov rax, [rbp - 864]
    ; Store to variable: pos
    mov r13d, eax       ; To register (int32/uint32)
    mov rax, 1
    mov r10, rax
    ; Load variable: col
    movsxd rax, r14d      ; From register (signed int32)
    add rax, r10
    mov [rbp - 872], rax
    mov rax, [rbp - 872]
    ; Store to variable: col
    mov r14d, eax       ; To register (int32/uint32)
    jmp ir_if_end_188
ir_if_next_194:
    cmp ebx, 123
    jne ir_if_next_199
    test r12, r12
    jnz ir_nonnull_201
ir_trap_null_200:
    ; IR call: puts (1 args)
    sub rsp, 32
    ; String literal (37 bytes)
    lea rax, [rel Lstr_struct79]  ; Load string struct address
    mov rcx, rax
    call puts
    add rsp, 32
    ; Unknown return type - assuming integer
    ; IR call: exit (1 args)
    sub rsp, 32
    mov rax, 1
    mov rcx, rax
    call exit
    add rsp, 32
    ; Unknown return type - assuming integer
ir_nonnull_201:
    mov rax, 0
    mov r10, rax
    ; Load variable: tok
    mov rax, r12      ; From register
    add rax, r10
    mov [rbp - 888], rax
    ; Load variable: TOKEN_LBRACE
    movsxd rax, dword [rel TOKEN_LBRACE]  ; From global memory
    mov rcx, rax
    mov rax, [rbp - 888]
    mov dword [rax], ecx
    test r12, r12
    jnz ir_nonnull_203
ir_trap_null_202:
    ; IR call: puts (1 args)
    sub rsp, 32
    ; String literal (37 bytes)
    lea rax, [rel Lstr_struct81]  ; Load string struct address
    mov rcx, rax
    call puts
    add rsp, 32
    ; Unknown return type - assuming integer
    ; IR call: exit (1 args)
    sub rsp, 32
    mov rax, 1
    mov rcx, rax
    call exit
    add rsp, 32
    ; Unknown return type - assuming integer
ir_nonnull_203:
    mov rax, 8
    mov r10, rax
    ; Load variable: tok
    mov rax, r12      ; From register
    add rax, r10
    mov [rbp - 904], rax
    mov rax, 1
    mov rcx, rax
    mov rax, [rbp - 904]
    mov dword [rax], ecx
    mov rax, 1
    mov r10, rax
    ; Load variable: pos
    movsxd rax, r13d      ; From register (signed int32)
    add rax, r10
    mov [rbp - 920], rax
    mov rax, [rbp - 920]
    ; Store to variable: pos
    mov r13d, eax       ; To register (int32/uint32)
    mov rax, 1
    mov r10, rax
    ; Load variable: col
    movsxd rax, r14d      ; From register (signed int32)
    add rax, r10
    mov [rbp - 928], rax
    mov rax, [rbp - 928]
    ; Store to variable: col
    mov r14d, eax       ; To register (int32/uint32)
    jmp ir_if_end_188
ir_if_next_199:
    cmp ebx, 125
    jne ir_if_next_204
    test r12, r12
    jnz ir_nonnull_206
ir_trap_null_205:
    ; IR call: puts (1 args)
    sub rsp, 32
    ; String literal (37 bytes)
    lea rax, [rel Lstr_struct83]  ; Load string struct address
    mov rcx, rax
    call puts
    add rsp, 32
    ; Unknown return type - assuming integer
    ; IR call: exit (1 args)
    sub rsp, 32
    mov rax, 1
    mov rcx, rax
    call exit
    add rsp, 32
    ; Unknown return type - assuming integer
ir_nonnull_206:
    mov rax, 0
    mov r10, rax
    ; Load variable: tok
    mov rax, r12      ; From register
    add rax, r10
    mov [rbp - 944], rax
    ; Load variable: TOKEN_RBRACE
    movsxd rax, dword [rel TOKEN_RBRACE]  ; From global memory
    mov rcx, rax
    mov rax, [rbp - 944]
    mov dword [rax], ecx
    test r12, r12
    jnz ir_nonnull_208
ir_trap_null_207:
    ; IR call: puts (1 args)
    sub rsp, 32
    ; String literal (37 bytes)
    lea rax, [rel Lstr_struct85]  ; Load string struct address
    mov rcx, rax
    call puts
    add rsp, 32
    ; Unknown return type - assuming integer
    ; IR call: exit (1 args)
    sub rsp, 32
    mov rax, 1
    mov rcx, rax
    call exit
    add rsp, 32
    ; Unknown return type - assuming integer
ir_nonnull_208:
    mov rax, 8
    mov r10, rax
    ; Load variable: tok
    mov rax, r12      ; From register
    add rax, r10
    mov [rbp - 960], rax
    mov rax, 1
    mov rcx, rax
    mov rax, [rbp - 960]
    mov dword [rax], ecx
    mov rax, 1
    mov r10, rax
    ; Load variable: pos
    movsxd rax, r13d      ; From register (signed int32)
    add rax, r10
    mov [rbp - 976], rax
    mov rax, [rbp - 976]
    ; Store to variable: pos
    mov r13d, eax       ; To register (int32/uint32)
    mov rax, 1
    mov r10, rax
    ; Load variable: col
    movsxd rax, r14d      ; From register (signed int32)
    add rax, r10
    mov [rbp - 984], rax
    mov rax, [rbp - 984]
    ; Store to variable: col
    mov r14d, eax       ; To register (int32/uint32)
    jmp ir_if_end_188
ir_if_next_204:
    cmp ebx, 59
    jne ir_if_next_209
    test r12, r12
    jnz ir_nonnull_211
ir_trap_null_210:
    ; IR call: puts (1 args)
    sub rsp, 32
    ; String literal (37 bytes)
    lea rax, [rel Lstr_struct87]  ; Load string struct address
    mov rcx, rax
    call puts
    add rsp, 32
    ; Unknown return type - assuming integer
    ; IR call: exit (1 args)
    sub rsp, 32
    mov rax, 1
    mov rcx, rax
    call exit
    add rsp, 32
    ; Unknown return type - assuming integer
ir_nonnull_211:
    mov rax, 0
    mov r10, rax
    ; Load variable: tok
    mov rax, r12      ; From register
    add rax, r10
    mov [rbp - 1000], rax
    ; Load variable: TOKEN_SEMI
    movsxd rax, dword [rel TOKEN_SEMI]  ; From global memory
    mov rcx, rax
    mov rax, [rbp - 1000]
    mov dword [rax], ecx
    test r12, r12
    jnz ir_nonnull_213
ir_trap_null_212:
    ; IR call: puts (1 args)
    sub rsp, 32
    ; String literal (37 bytes)
    lea rax, [rel Lstr_struct89]  ; Load string struct address
    mov rcx, rax
    call puts
    add rsp, 32
    ; Unknown return type - assuming integer
    ; IR call: exit (1 args)
    sub rsp, 32
    mov rax, 1
    mov rcx, rax
    call exit
    add rsp, 32
    ; Unknown return type - assuming integer
ir_nonnull_213:
    mov rax, 8
    mov r10, rax
    ; Load variable: tok
    mov rax, r12      ; From register
    add rax, r10
    mov [rbp - 1016], rax
    mov rax, 1
    mov rcx, rax
    mov rax, [rbp - 1016]
    mov dword [rax], ecx
    mov rax, 1
    mov r10, rax
    ; Load variable: pos
    movsxd rax, r13d      ; From register (signed int32)
    add rax, r10
    mov [rbp - 1032], rax
    mov rax, [rbp - 1032]
    ; Store to variable: pos
    mov r13d, eax       ; To register (int32/uint32)
    mov rax, 1
    mov r10, rax
    ; Load variable: col
    movsxd rax, r14d      ; From register (signed int32)
    add rax, r10
    mov [rbp - 1040], rax
    mov rax, [rbp - 1040]
    ; Store to variable: col
    mov r14d, eax       ; To register (int32/uint32)
    jmp ir_if_end_188
ir_if_next_209:
    cmp ebx, 58
    jne ir_if_next_214
    test r12, r12
    jnz ir_nonnull_216
ir_trap_null_215:
    ; IR call: puts (1 args)
    sub rsp, 32
    ; String literal (37 bytes)
    lea rax, [rel Lstr_struct91]  ; Load string struct address
    mov rcx, rax
    call puts
    add rsp, 32
    ; Unknown return type - assuming integer
    ; IR call: exit (1 args)
    sub rsp, 32
    mov rax, 1
    mov rcx, rax
    call exit
    add rsp, 32
    ; Unknown return type - assuming integer
ir_nonnull_216:
    mov rax, 0
    mov r10, rax
    ; Load variable: tok
    mov rax, r12      ; From register
    add rax, r10
    mov [rbp - 1056], rax
    ; Load variable: TOKEN_COLON
    movsxd rax, dword [rel TOKEN_COLON]  ; From global memory
    mov rcx, rax
    mov rax, [rbp - 1056]
    mov dword [rax], ecx
    test r12, r12
    jnz ir_nonnull_218
ir_trap_null_217:
    ; IR call: puts (1 args)
    sub rsp, 32
    ; String literal (37 bytes)
    lea rax, [rel Lstr_struct93]  ; Load string struct address
    mov rcx, rax
    call puts
    add rsp, 32
    ; Unknown return type - assuming integer
    ; IR call: exit (1 args)
    sub rsp, 32
    mov rax, 1
    mov rcx, rax
    call exit
    add rsp, 32
    ; Unknown return type - assuming integer
ir_nonnull_218:
    mov rax, 8
    mov r10, rax
    ; Load variable: tok
    mov rax, r12      ; From register
    add rax, r10
    mov [rbp - 1072], rax
    mov rax, 1
    mov rcx, rax
    mov rax, [rbp - 1072]
    mov dword [rax], ecx
    mov rax, 1
    mov r10, rax
    ; Load variable: pos
    movsxd rax, r13d      ; From register (signed int32)
    add rax, r10
    mov [rbp - 1088], rax
    mov rax, [rbp - 1088]
    ; Store to variable: pos
    mov r13d, eax       ; To register (int32/uint32)
    mov rax, 1
    mov r10, rax
    ; Load variable: col
    movsxd rax, r14d      ; From register (signed int32)
    add rax, r10
    mov [rbp - 1096], rax
    mov rax, [rbp - 1096]
    ; Store to variable: col
    mov r14d, eax       ; To register (int32/uint32)
    jmp ir_if_end_188
ir_if_next_214:
    cmp ebx, 44
    jne ir_if_next_219
    test r12, r12
    jnz ir_nonnull_221
ir_trap_null_220:
    ; IR call: puts (1 args)
    sub rsp, 32
    ; String literal (37 bytes)
    lea rax, [rel Lstr_struct95]  ; Load string struct address
    mov rcx, rax
    call puts
    add rsp, 32
    ; Unknown return type - assuming integer
    ; IR call: exit (1 args)
    sub rsp, 32
    mov rax, 1
    mov rcx, rax
    call exit
    add rsp, 32
    ; Unknown return type - assuming integer
ir_nonnull_221:
    mov rax, 0
    mov r10, rax
    ; Load variable: tok
    mov rax, r12      ; From register
    add rax, r10
    mov [rbp - 1112], rax
    ; Load variable: TOKEN_COMMA
    movsxd rax, dword [rel TOKEN_COMMA]  ; From global memory
    mov rcx, rax
    mov rax, [rbp - 1112]
    mov dword [rax], ecx
    test r12, r12
    jnz ir_nonnull_223
ir_trap_null_222:
    ; IR call: puts (1 args)
    sub rsp, 32
    ; String literal (37 bytes)
    lea rax, [rel Lstr_struct97]  ; Load string struct address
    mov rcx, rax
    call puts
    add rsp, 32
    ; Unknown return type - assuming integer
    ; IR call: exit (1 args)
    sub rsp, 32
    mov rax, 1
    mov rcx, rax
    call exit
    add rsp, 32
    ; Unknown return type - assuming integer
ir_nonnull_223:
    mov rax, 8
    mov r10, rax
    ; Load variable: tok
    mov rax, r12      ; From register
    add rax, r10
    mov [rbp - 1128], rax
    mov rax, 1
    mov rcx, rax
    mov rax, [rbp - 1128]
    mov dword [rax], ecx
    mov rax, 1
    mov r10, rax
    ; Load variable: pos
    movsxd rax, r13d      ; From register (signed int32)
    add rax, r10
    mov [rbp - 1144], rax
    mov rax, [rbp - 1144]
    ; Store to variable: pos
    mov r13d, eax       ; To register (int32/uint32)
    mov rax, 1
    mov r10, rax
    ; Load variable: col
    movsxd rax, r14d      ; From register (signed int32)
    add rax, r10
    mov [rbp - 1152], rax
    mov rax, [rbp - 1152]
    ; Store to variable: col
    mov r14d, eax       ; To register (int32/uint32)
    jmp ir_if_end_188
ir_if_next_219:
    cmp ebx, 43
    jne ir_if_next_224
    test r12, r12
    jnz ir_nonnull_226
ir_trap_null_225:
    ; IR call: puts (1 args)
    sub rsp, 32
    ; String literal (37 bytes)
    lea rax, [rel Lstr_struct99]  ; Load string struct address
    mov rcx, rax
    call puts
    add rsp, 32
    ; Unknown return type - assuming integer
    ; IR call: exit (1 args)
    sub rsp, 32
    mov rax, 1
    mov rcx, rax
    call exit
    add rsp, 32
    ; Unknown return type - assuming integer
ir_nonnull_226:
    mov rax, 0
    mov r10, rax
    ; Load variable: tok
    mov rax, r12      ; From register
    add rax, r10
    mov [rbp - 1168], rax
    ; Load variable: TOKEN_PLUS
    movsxd rax, dword [rel TOKEN_PLUS]  ; From global memory
    mov rcx, rax
    mov rax, [rbp - 1168]
    mov dword [rax], ecx
    test r12, r12
    jnz ir_nonnull_228
ir_trap_null_227:
    ; IR call: puts (1 args)
    sub rsp, 32
    ; String literal (37 bytes)
    lea rax, [rel Lstr_struct101]  ; Load string struct address
    mov rcx, rax
    call puts
    add rsp, 32
    ; Unknown return type - assuming integer
    ; IR call: exit (1 args)
    sub rsp, 32
    mov rax, 1
    mov rcx, rax
    call exit
    add rsp, 32
    ; Unknown return type - assuming integer
ir_nonnull_228:
    mov rax, 8
    mov r10, rax
    ; Load variable: tok
    mov rax, r12      ; From register
    add rax, r10
    mov [rbp - 1184], rax
    mov rax, 1
    mov rcx, rax
    mov rax, [rbp - 1184]
    mov dword [rax], ecx
    mov rax, 1
    mov r10, rax
    ; Load variable: pos
    movsxd rax, r13d      ; From register (signed int32)
    add rax, r10
    mov [rbp - 1200], rax
    mov rax, [rbp - 1200]
    ; Store to variable: pos
    mov r13d, eax       ; To register (int32/uint32)
    mov rax, 1
    mov r10, rax
    ; Load variable: col
    movsxd rax, r14d      ; From register (signed int32)
    add rax, r10
    mov [rbp - 1208], rax
    mov rax, [rbp - 1208]
    ; Store to variable: col
    mov r14d, eax       ; To register (int32/uint32)
    jmp ir_if_end_188
ir_if_next_224:
    cmp ebx, 45
    jne ir_if_next_229
    mov rax, 1
    mov r10, rax
    ; Load variable: pos
    movsxd rax, r13d      ; From register (signed int32)
    add rax, r10
    mov [rbp - 1224], rax
    test r15, r15
    jnz ir_nonnull_233
ir_trap_null_232:
    ; IR call: puts (1 args)
    sub rsp, 32
    ; String literal (37 bytes)
    lea rax, [rel Lstr_struct103]  ; Load string struct address
    mov rcx, rax
    call puts
    add rsp, 32
    ; Unknown return type - assuming integer
    ; IR call: exit (1 args)
    sub rsp, 32
    mov rax, 1
    mov rcx, rax
    call exit
    add rsp, 32
    ; Unknown return type - assuming integer
ir_nonnull_233:
    mov rax, 1
    mov r10, rax
    mov rax, [rbp - 1224]
    imul rax, r10
    mov [rbp - 1232], rax
    mov rax, [rbp - 1232]
    mov r10, rax
    ; Load variable: source
    mov rax, r15      ; From register
    add rax, r10
    mov [rbp - 1240], rax
    mov rax, [rbp - 1240]
    movzx rax, byte [rax]
    mov [rbp - 1248], rax
    mov rax, 62
    mov r10, rax
    mov rax, [rbp - 1248]
    cmp rax, r10
    jne ir_if_next_231
    test r12, r12
    jnz ir_nonnull_235
ir_trap_null_234:
    ; IR call: puts (1 args)
    sub rsp, 32
    ; String literal (37 bytes)
    lea rax, [rel Lstr_struct105]  ; Load string struct address
    mov rcx, rax
    call puts
    add rsp, 32
    ; Unknown return type - assuming integer
    ; IR call: exit (1 args)
    sub rsp, 32
    mov rax, 1
    mov rcx, rax
    call exit
    add rsp, 32
    ; Unknown return type - assuming integer
ir_nonnull_235:
    mov rax, 0
    mov r10, rax
    ; Load variable: tok
    mov rax, r12      ; From register
    add rax, r10
    mov [rbp - 1264], rax
    ; Load variable: TOKEN_ARROW
    movsxd rax, dword [rel TOKEN_ARROW]  ; From global memory
    mov rcx, rax
    mov rax, [rbp - 1264]
    mov dword [rax], ecx
    test r12, r12
    jnz ir_nonnull_237
ir_trap_null_236:
    ; IR call: puts (1 args)
    sub rsp, 32
    ; String literal (37 bytes)
    lea rax, [rel Lstr_struct107]  ; Load string struct address
    mov rcx, rax
    call puts
    add rsp, 32
    ; Unknown return type - assuming integer
    ; IR call: exit (1 args)
    sub rsp, 32
    mov rax, 1
    mov rcx, rax
    call exit
    add rsp, 32
    ; Unknown return type - assuming integer
ir_nonnull_237:
    mov rax, 8
    mov r10, rax
    ; Load variable: tok
    mov rax, r12      ; From register
    add rax, r10
    mov [rbp - 1280], rax
    mov rax, 2
    mov rcx, rax
    mov rax, [rbp - 1280]
    mov dword [rax], ecx
    mov rax, 2
    mov r10, rax
    ; Load variable: pos
    movsxd rax, r13d      ; From register (signed int32)
    add rax, r10
    mov [rbp - 1296], rax
    mov rax, [rbp - 1296]
    ; Store to variable: pos
    mov r13d, eax       ; To register (int32/uint32)
    mov rax, 2
    mov r10, rax
    ; Load variable: col
    movsxd rax, r14d      ; From register (signed int32)
    add rax, r10
    mov [rbp - 1304], rax
    mov rax, [rbp - 1304]
    ; Store to variable: col
    mov r14d, eax       ; To register (int32/uint32)
    jmp ir_if_end_230
ir_if_next_231:
    test r12, r12
    jnz ir_nonnull_239
ir_trap_null_238:
    ; IR call: puts (1 args)
    sub rsp, 32
    ; String literal (37 bytes)
    lea rax, [rel Lstr_struct109]  ; Load string struct address
    mov rcx, rax
    call puts
    add rsp, 32
    ; Unknown return type - assuming integer
    ; IR call: exit (1 args)
    sub rsp, 32
    mov rax, 1
    mov rcx, rax
    call exit
    add rsp, 32
    ; Unknown return type - assuming integer
ir_nonnull_239:
    mov rax, 0
    mov r10, rax
    ; Load variable: tok
    mov rax, r12      ; From register
    add rax, r10
    mov [rbp - 1312], rax
    ; Load variable: TOKEN_MINUS
    movsxd rax, dword [rel TOKEN_MINUS]  ; From global memory
    mov rcx, rax
    mov rax, [rbp - 1312]
    mov dword [rax], ecx
    test r12, r12
    jnz ir_nonnull_241
ir_trap_null_240:
    ; IR call: puts (1 args)
    sub rsp, 32
    ; String literal (37 bytes)
    lea rax, [rel Lstr_struct111]  ; Load string struct address
    mov rcx, rax
    call puts
    add rsp, 32
    ; Unknown return type - assuming integer
    ; IR call: exit (1 args)
    sub rsp, 32
    mov rax, 1
    mov rcx, rax
    call exit
    add rsp, 32
    ; Unknown return type - assuming integer
ir_nonnull_241:
    mov rax, 8
    mov r10, rax
    ; Load variable: tok
    mov rax, r12      ; From register
    add rax, r10
    mov [rbp - 1328], rax
    mov rax, 1
    mov rcx, rax
    mov rax, [rbp - 1328]
    mov dword [rax], ecx
    mov rax, 1
    mov r10, rax
    ; Load variable: pos
    movsxd rax, r13d      ; From register (signed int32)
    add rax, r10
    mov [rbp - 1344], rax
    mov rax, [rbp - 1344]
    ; Store to variable: pos
    mov r13d, eax       ; To register (int32/uint32)
    mov rax, 1
    mov r10, rax
    ; Load variable: col
    movsxd rax, r14d      ; From register (signed int32)
    add rax, r10
    mov [rbp - 1352], rax
    mov rax, [rbp - 1352]
    ; Store to variable: col
    mov r14d, eax       ; To register (int32/uint32)
ir_if_end_230:
    jmp ir_if_end_188
ir_if_next_229:
    cmp ebx, 42
    jne ir_if_next_242
    test r12, r12
    jnz ir_nonnull_244
ir_trap_null_243:
    ; IR call: puts (1 args)
    sub rsp, 32
    ; String literal (37 bytes)
    lea rax, [rel Lstr_struct113]  ; Load string struct address
    mov rcx, rax
    call puts
    add rsp, 32
    ; Unknown return type - assuming integer
    ; IR call: exit (1 args)
    sub rsp, 32
    mov rax, 1
    mov rcx, rax
    call exit
    add rsp, 32
    ; Unknown return type - assuming integer
ir_nonnull_244:
    mov rax, 0
    mov r10, rax
    ; Load variable: tok
    mov rax, r12      ; From register
    add rax, r10
    mov [rbp - 1368], rax
    ; Load variable: TOKEN_STAR
    movsxd rax, dword [rel TOKEN_STAR]  ; From global memory
    mov rcx, rax
    mov rax, [rbp - 1368]
    mov dword [rax], ecx
    test r12, r12
    jnz ir_nonnull_246
ir_trap_null_245:
    ; IR call: puts (1 args)
    sub rsp, 32
    ; String literal (37 bytes)
    lea rax, [rel Lstr_struct115]  ; Load string struct address
    mov rcx, rax
    call puts
    add rsp, 32
    ; Unknown return type - assuming integer
    ; IR call: exit (1 args)
    sub rsp, 32
    mov rax, 1
    mov rcx, rax
    call exit
    add rsp, 32
    ; Unknown return type - assuming integer
ir_nonnull_246:
    mov rax, 8
    mov r10, rax
    ; Load variable: tok
    mov rax, r12      ; From register
    add rax, r10
    mov [rbp - 1384], rax
    mov rax, 1
    mov rcx, rax
    mov rax, [rbp - 1384]
    mov dword [rax], ecx
    mov rax, 1
    mov r10, rax
    ; Load variable: pos
    movsxd rax, r13d      ; From register (signed int32)
    add rax, r10
    mov [rbp - 1400], rax
    mov rax, [rbp - 1400]
    ; Store to variable: pos
    mov r13d, eax       ; To register (int32/uint32)
    mov rax, 1
    mov r10, rax
    ; Load variable: col
    movsxd rax, r14d      ; From register (signed int32)
    add rax, r10
    mov [rbp - 1408], rax
    mov rax, [rbp - 1408]
    ; Store to variable: col
    mov r14d, eax       ; To register (int32/uint32)
    jmp ir_if_end_188
ir_if_next_242:
    cmp ebx, 37
    jne ir_if_next_247
    test r12, r12
    jnz ir_nonnull_249
ir_trap_null_248:
    ; IR call: puts (1 args)
    sub rsp, 32
    ; String literal (37 bytes)
    lea rax, [rel Lstr_struct117]  ; Load string struct address
    mov rcx, rax
    call puts
    add rsp, 32
    ; Unknown return type - assuming integer
    ; IR call: exit (1 args)
    sub rsp, 32
    mov rax, 1
    mov rcx, rax
    call exit
    add rsp, 32
    ; Unknown return type - assuming integer
ir_nonnull_249:
    mov rax, 0
    mov r10, rax
    ; Load variable: tok
    mov rax, r12      ; From register
    add rax, r10
    mov [rbp - 1424], rax
    ; Load variable: TOKEN_PERCENT
    movsxd rax, dword [rel TOKEN_PERCENT]  ; From global memory
    mov rcx, rax
    mov rax, [rbp - 1424]
    mov dword [rax], ecx
    test r12, r12
    jnz ir_nonnull_251
ir_trap_null_250:
    ; IR call: puts (1 args)
    sub rsp, 32
    ; String literal (37 bytes)
    lea rax, [rel Lstr_struct119]  ; Load string struct address
    mov rcx, rax
    call puts
    add rsp, 32
    ; Unknown return type - assuming integer
    ; IR call: exit (1 args)
    sub rsp, 32
    mov rax, 1
    mov rcx, rax
    call exit
    add rsp, 32
    ; Unknown return type - assuming integer
ir_nonnull_251:
    mov rax, 8
    mov r10, rax
    ; Load variable: tok
    mov rax, r12      ; From register
    add rax, r10
    mov [rbp - 1440], rax
    mov rax, 1
    mov rcx, rax
    mov rax, [rbp - 1440]
    mov dword [rax], ecx
    mov rax, 1
    mov r10, rax
    ; Load variable: pos
    movsxd rax, r13d      ; From register (signed int32)
    add rax, r10
    mov [rbp - 1456], rax
    mov rax, [rbp - 1456]
    ; Store to variable: pos
    mov r13d, eax       ; To register (int32/uint32)
    mov rax, 1
    mov r10, rax
    ; Load variable: col
    movsxd rax, r14d      ; From register (signed int32)
    add rax, r10
    mov [rbp - 1464], rax
    mov rax, [rbp - 1464]
    ; Store to variable: col
    mov r14d, eax       ; To register (int32/uint32)
    jmp ir_if_end_188
ir_if_next_247:
    cmp ebx, 61
    jne ir_if_next_252
    mov rax, 1
    mov r10, rax
    ; Load variable: pos
    movsxd rax, r13d      ; From register (signed int32)
    add rax, r10
    mov [rbp - 1480], rax
    test r15, r15
    jnz ir_nonnull_256
ir_trap_null_255:
    ; IR call: puts (1 args)
    sub rsp, 32
    ; String literal (37 bytes)
    lea rax, [rel Lstr_struct121]  ; Load string struct address
    mov rcx, rax
    call puts
    add rsp, 32
    ; Unknown return type - assuming integer
    ; IR call: exit (1 args)
    sub rsp, 32
    mov rax, 1
    mov rcx, rax
    call exit
    add rsp, 32
    ; Unknown return type - assuming integer
ir_nonnull_256:
    mov rax, 1
    mov r10, rax
    mov rax, [rbp - 1480]
    imul rax, r10
    mov [rbp - 1488], rax
    mov rax, [rbp - 1488]
    mov r10, rax
    ; Load variable: source
    mov rax, r15      ; From register
    add rax, r10
    mov [rbp - 1496], rax
    mov rax, [rbp - 1496]
    movzx rax, byte [rax]
    mov [rbp - 1504], rax
    mov rax, 61
    mov r10, rax
    mov rax, [rbp - 1504]
    cmp rax, r10
    jne ir_if_next_254
    test r12, r12
    jnz ir_nonnull_258
ir_trap_null_257:
    ; IR call: puts (1 args)
    sub rsp, 32
    ; String literal (37 bytes)
    lea rax, [rel Lstr_struct123]  ; Load string struct address
    mov rcx, rax
    call puts
    add rsp, 32
    ; Unknown return type - assuming integer
    ; IR call: exit (1 args)
    sub rsp, 32
    mov rax, 1
    mov rcx, rax
    call exit
    add rsp, 32
    ; Unknown return type - assuming integer
ir_nonnull_258:
    mov rax, 0
    mov r10, rax
    ; Load variable: tok
    mov rax, r12      ; From register
    add rax, r10
    mov [rbp - 1520], rax
    ; Load variable: TOKEN_EQEQ
    movsxd rax, dword [rel TOKEN_EQEQ]  ; From global memory
    mov rcx, rax
    mov rax, [rbp - 1520]
    mov dword [rax], ecx
    test r12, r12
    jnz ir_nonnull_260
ir_trap_null_259:
    ; IR call: puts (1 args)
    sub rsp, 32
    ; String literal (37 bytes)
    lea rax, [rel Lstr_struct125]  ; Load string struct address
    mov rcx, rax
    call puts
    add rsp, 32
    ; Unknown return type - assuming integer
    ; IR call: exit (1 args)
    sub rsp, 32
    mov rax, 1
    mov rcx, rax
    call exit
    add rsp, 32
    ; Unknown return type - assuming integer
ir_nonnull_260:
    mov rax, 8
    mov r10, rax
    ; Load variable: tok
    mov rax, r12      ; From register
    add rax, r10
    mov [rbp - 1536], rax
    mov rax, 2
    mov rcx, rax
    mov rax, [rbp - 1536]
    mov dword [rax], ecx
    mov rax, 2
    mov r10, rax
    ; Load variable: pos
    movsxd rax, r13d      ; From register (signed int32)
    add rax, r10
    mov [rbp - 1552], rax
    mov rax, [rbp - 1552]
    ; Store to variable: pos
    mov r13d, eax       ; To register (int32/uint32)
    mov rax, 2
    mov r10, rax
    ; Load variable: col
    movsxd rax, r14d      ; From register (signed int32)
    add rax, r10
    mov [rbp - 1560], rax
    mov rax, [rbp - 1560]
    ; Store to variable: col
    mov r14d, eax       ; To register (int32/uint32)
    jmp ir_if_end_253
ir_if_next_254:
    test r12, r12
    jnz ir_nonnull_262
ir_trap_null_261:
    ; IR call: puts (1 args)
    sub rsp, 32
    ; String literal (37 bytes)
    lea rax, [rel Lstr_struct127]  ; Load string struct address
    mov rcx, rax
    call puts
    add rsp, 32
    ; Unknown return type - assuming integer
    ; IR call: exit (1 args)
    sub rsp, 32
    mov rax, 1
    mov rcx, rax
    call exit
    add rsp, 32
    ; Unknown return type - assuming integer
ir_nonnull_262:
    mov rax, 0
    mov r10, rax
    ; Load variable: tok
    mov rax, r12      ; From register
    add rax, r10
    mov [rbp - 1568], rax
    ; Load variable: TOKEN_EQ
    movsxd rax, dword [rel TOKEN_EQ]  ; From global memory
    mov rcx, rax
    mov rax, [rbp - 1568]
    mov dword [rax], ecx
    test r12, r12
    jnz ir_nonnull_264
ir_trap_null_263:
    ; IR call: puts (1 args)
    sub rsp, 32
    ; String literal (37 bytes)
    lea rax, [rel Lstr_struct129]  ; Load string struct address
    mov rcx, rax
    call puts
    add rsp, 32
    ; Unknown return type - assuming integer
    ; IR call: exit (1 args)
    sub rsp, 32
    mov rax, 1
    mov rcx, rax
    call exit
    add rsp, 32
    ; Unknown return type - assuming integer
ir_nonnull_264:
    mov rax, 8
    mov r10, rax
    ; Load variable: tok
    mov rax, r12      ; From register
    add rax, r10
    mov [rbp - 1584], rax
    mov rax, 1
    mov rcx, rax
    mov rax, [rbp - 1584]
    mov dword [rax], ecx
    mov rax, 1
    mov r10, rax
    ; Load variable: pos
    movsxd rax, r13d      ; From register (signed int32)
    add rax, r10
    mov [rbp - 1600], rax
    mov rax, [rbp - 1600]
    ; Store to variable: pos
    mov r13d, eax       ; To register (int32/uint32)
    mov rax, 1
    mov r10, rax
    ; Load variable: col
    movsxd rax, r14d      ; From register (signed int32)
    add rax, r10
    mov [rbp - 1608], rax
    mov rax, [rbp - 1608]
    ; Store to variable: col
    mov r14d, eax       ; To register (int32/uint32)
ir_if_end_253:
    jmp ir_if_end_188
ir_if_next_252:
    cmp ebx, 33
    jne ir_if_next_265
    mov rax, 1
    mov r10, rax
    ; Load variable: pos
    movsxd rax, r13d      ; From register (signed int32)
    add rax, r10
    mov [rbp - 1624], rax
    test r15, r15
    jnz ir_nonnull_269
ir_trap_null_268:
    ; IR call: puts (1 args)
    sub rsp, 32
    ; String literal (37 bytes)
    lea rax, [rel Lstr_struct131]  ; Load string struct address
    mov rcx, rax
    call puts
    add rsp, 32
    ; Unknown return type - assuming integer
    ; IR call: exit (1 args)
    sub rsp, 32
    mov rax, 1
    mov rcx, rax
    call exit
    add rsp, 32
    ; Unknown return type - assuming integer
ir_nonnull_269:
    mov rax, 1
    mov r10, rax
    mov rax, [rbp - 1624]
    imul rax, r10
    mov [rbp - 1632], rax
    mov rax, [rbp - 1632]
    mov r10, rax
    ; Load variable: source
    mov rax, r15      ; From register
    add rax, r10
    mov [rbp - 1640], rax
    mov rax, [rbp - 1640]
    movzx rax, byte [rax]
    mov [rbp - 1648], rax
    mov rax, 61
    mov r10, rax
    mov rax, [rbp - 1648]
    cmp rax, r10
    jne ir_if_next_267
    test r12, r12
    jnz ir_nonnull_271
ir_trap_null_270:
    ; IR call: puts (1 args)
    sub rsp, 32
    ; String literal (37 bytes)
    lea rax, [rel Lstr_struct133]  ; Load string struct address
    mov rcx, rax
    call puts
    add rsp, 32
    ; Unknown return type - assuming integer
    ; IR call: exit (1 args)
    sub rsp, 32
    mov rax, 1
    mov rcx, rax
    call exit
    add rsp, 32
    ; Unknown return type - assuming integer
ir_nonnull_271:
    mov rax, 0
    mov r10, rax
    ; Load variable: tok
    mov rax, r12      ; From register
    add rax, r10
    mov [rbp - 1664], rax
    ; Load variable: TOKEN_NE
    movsxd rax, dword [rel TOKEN_NE]  ; From global memory
    mov rcx, rax
    mov rax, [rbp - 1664]
    mov dword [rax], ecx
    test r12, r12
    jnz ir_nonnull_273
ir_trap_null_272:
    ; IR call: puts (1 args)
    sub rsp, 32
    ; String literal (37 bytes)
    lea rax, [rel Lstr_struct135]  ; Load string struct address
    mov rcx, rax
    call puts
    add rsp, 32
    ; Unknown return type - assuming integer
    ; IR call: exit (1 args)
    sub rsp, 32
    mov rax, 1
    mov rcx, rax
    call exit
    add rsp, 32
    ; Unknown return type - assuming integer
ir_nonnull_273:
    mov rax, 8
    mov r10, rax
    ; Load variable: tok
    mov rax, r12      ; From register
    add rax, r10
    mov [rbp - 1680], rax
    mov rax, 2
    mov rcx, rax
    mov rax, [rbp - 1680]
    mov dword [rax], ecx
    mov rax, 2
    mov r10, rax
    ; Load variable: pos
    movsxd rax, r13d      ; From register (signed int32)
    add rax, r10
    mov [rbp - 1696], rax
    mov rax, [rbp - 1696]
    ; Store to variable: pos
    mov r13d, eax       ; To register (int32/uint32)
    mov rax, 2
    mov r10, rax
    ; Load variable: col
    movsxd rax, r14d      ; From register (signed int32)
    add rax, r10
    mov [rbp - 1704], rax
    mov rax, [rbp - 1704]
    ; Store to variable: col
    mov r14d, eax       ; To register (int32/uint32)
    jmp ir_if_end_266
ir_if_next_267:
    test r12, r12
    jnz ir_nonnull_275
ir_trap_null_274:
    ; IR call: puts (1 args)
    sub rsp, 32
    ; String literal (37 bytes)
    lea rax, [rel Lstr_struct137]  ; Load string struct address
    mov rcx, rax
    call puts
    add rsp, 32
    ; Unknown return type - assuming integer
    ; IR call: exit (1 args)
    sub rsp, 32
    mov rax, 1
    mov rcx, rax
    call exit
    add rsp, 32
    ; Unknown return type - assuming integer
ir_nonnull_275:
    mov rax, 0
    mov r10, rax
    ; Load variable: tok
    mov rax, r12      ; From register
    add rax, r10
    mov [rbp - 1712], rax
    ; Load variable: TOKEN_NOT
    movsxd rax, dword [rel TOKEN_NOT]  ; From global memory
    mov rcx, rax
    mov rax, [rbp - 1712]
    mov dword [rax], ecx
    test r12, r12
    jnz ir_nonnull_277
ir_trap_null_276:
    ; IR call: puts (1 args)
    sub rsp, 32
    ; String literal (37 bytes)
    lea rax, [rel Lstr_struct139]  ; Load string struct address
    mov rcx, rax
    call puts
    add rsp, 32
    ; Unknown return type - assuming integer
    ; IR call: exit (1 args)
    sub rsp, 32
    mov rax, 1
    mov rcx, rax
    call exit
    add rsp, 32
    ; Unknown return type - assuming integer
ir_nonnull_277:
    mov rax, 8
    mov r10, rax
    ; Load variable: tok
    mov rax, r12      ; From register
    add rax, r10
    mov [rbp - 1728], rax
    mov rax, 1
    mov rcx, rax
    mov rax, [rbp - 1728]
    mov dword [rax], ecx
    mov rax, 1
    mov r10, rax
    ; Load variable: pos
    movsxd rax, r13d      ; From register (signed int32)
    add rax, r10
    mov [rbp - 1744], rax
    mov rax, [rbp - 1744]
    ; Store to variable: pos
    mov r13d, eax       ; To register (int32/uint32)
    mov rax, 1
    mov r10, rax
    ; Load variable: col
    movsxd rax, r14d      ; From register (signed int32)
    add rax, r10
    mov [rbp - 1752], rax
    mov rax, [rbp - 1752]
    ; Store to variable: col
    mov r14d, eax       ; To register (int32/uint32)
ir_if_end_266:
    jmp ir_if_end_188
ir_if_next_265:
    cmp ebx, 60
    jne ir_if_next_278
    mov rax, 1
    mov r10, rax
    ; Load variable: pos
    movsxd rax, r13d      ; From register (signed int32)
    add rax, r10
    mov [rbp - 1768], rax
    test r15, r15
    jnz ir_nonnull_282
ir_trap_null_281:
    ; IR call: puts (1 args)
    sub rsp, 32
    ; String literal (37 bytes)
    lea rax, [rel Lstr_struct141]  ; Load string struct address
    mov rcx, rax
    call puts
    add rsp, 32
    ; Unknown return type - assuming integer
    ; IR call: exit (1 args)
    sub rsp, 32
    mov rax, 1
    mov rcx, rax
    call exit
    add rsp, 32
    ; Unknown return type - assuming integer
ir_nonnull_282:
    mov rax, 1
    mov r10, rax
    mov rax, [rbp - 1768]
    imul rax, r10
    mov [rbp - 1776], rax
    mov rax, [rbp - 1776]
    mov r10, rax
    ; Load variable: source
    mov rax, r15      ; From register
    add rax, r10
    mov [rbp - 1784], rax
    mov rax, [rbp - 1784]
    movzx rax, byte [rax]
    mov [rbp - 1792], rax
    mov rax, 61
    mov r10, rax
    mov rax, [rbp - 1792]
    cmp rax, r10
    jne ir_if_next_280
    test r12, r12
    jnz ir_nonnull_284
ir_trap_null_283:
    ; IR call: puts (1 args)
    sub rsp, 32
    ; String literal (37 bytes)
    lea rax, [rel Lstr_struct143]  ; Load string struct address
    mov rcx, rax
    call puts
    add rsp, 32
    ; Unknown return type - assuming integer
    ; IR call: exit (1 args)
    sub rsp, 32
    mov rax, 1
    mov rcx, rax
    call exit
    add rsp, 32
    ; Unknown return type - assuming integer
ir_nonnull_284:
    mov rax, 0
    mov r10, rax
    ; Load variable: tok
    mov rax, r12      ; From register
    add rax, r10
    mov [rbp - 1808], rax
    ; Load variable: TOKEN_LE
    movsxd rax, dword [rel TOKEN_LE]  ; From global memory
    mov rcx, rax
    mov rax, [rbp - 1808]
    mov dword [rax], ecx
    test r12, r12
    jnz ir_nonnull_286
ir_trap_null_285:
    ; IR call: puts (1 args)
    sub rsp, 32
    ; String literal (37 bytes)
    lea rax, [rel Lstr_struct145]  ; Load string struct address
    mov rcx, rax
    call puts
    add rsp, 32
    ; Unknown return type - assuming integer
    ; IR call: exit (1 args)
    sub rsp, 32
    mov rax, 1
    mov rcx, rax
    call exit
    add rsp, 32
    ; Unknown return type - assuming integer
ir_nonnull_286:
    mov rax, 8
    mov r10, rax
    ; Load variable: tok
    mov rax, r12      ; From register
    add rax, r10
    mov [rbp - 1824], rax
    mov rax, 2
    mov rcx, rax
    mov rax, [rbp - 1824]
    mov dword [rax], ecx
    mov rax, 2
    mov r10, rax
    ; Load variable: pos
    movsxd rax, r13d      ; From register (signed int32)
    add rax, r10
    mov [rbp - 1840], rax
    mov rax, [rbp - 1840]
    ; Store to variable: pos
    mov r13d, eax       ; To register (int32/uint32)
    mov rax, 2
    mov r10, rax
    ; Load variable: col
    movsxd rax, r14d      ; From register (signed int32)
    add rax, r10
    mov [rbp - 1848], rax
    mov rax, [rbp - 1848]
    ; Store to variable: col
    mov r14d, eax       ; To register (int32/uint32)
    jmp ir_if_end_279
ir_if_next_280:
    test r12, r12
    jnz ir_nonnull_288
ir_trap_null_287:
    ; IR call: puts (1 args)
    sub rsp, 32
    ; String literal (37 bytes)
    lea rax, [rel Lstr_struct147]  ; Load string struct address
    mov rcx, rax
    call puts
    add rsp, 32
    ; Unknown return type - assuming integer
    ; IR call: exit (1 args)
    sub rsp, 32
    mov rax, 1
    mov rcx, rax
    call exit
    add rsp, 32
    ; Unknown return type - assuming integer
ir_nonnull_288:
    mov rax, 0
    mov r10, rax
    ; Load variable: tok
    mov rax, r12      ; From register
    add rax, r10
    mov [rbp - 1856], rax
    ; Load variable: TOKEN_LT
    movsxd rax, dword [rel TOKEN_LT]  ; From global memory
    mov rcx, rax
    mov rax, [rbp - 1856]
    mov dword [rax], ecx
    test r12, r12
    jnz ir_nonnull_290
ir_trap_null_289:
    ; IR call: puts (1 args)
    sub rsp, 32
    ; String literal (37 bytes)
    lea rax, [rel Lstr_struct149]  ; Load string struct address
    mov rcx, rax
    call puts
    add rsp, 32
    ; Unknown return type - assuming integer
    ; IR call: exit (1 args)
    sub rsp, 32
    mov rax, 1
    mov rcx, rax
    call exit
    add rsp, 32
    ; Unknown return type - assuming integer
ir_nonnull_290:
    mov rax, 8
    mov r10, rax
    ; Load variable: tok
    mov rax, r12      ; From register
    add rax, r10
    mov [rbp - 1872], rax
    mov rax, 1
    mov rcx, rax
    mov rax, [rbp - 1872]
    mov dword [rax], ecx
    mov rax, 1
    mov r10, rax
    ; Load variable: pos
    movsxd rax, r13d      ; From register (signed int32)
    add rax, r10
    mov [rbp - 1888], rax
    mov rax, [rbp - 1888]
    ; Store to variable: pos
    mov r13d, eax       ; To register (int32/uint32)
    mov rax, 1
    mov r10, rax
    ; Load variable: col
    movsxd rax, r14d      ; From register (signed int32)
    add rax, r10
    mov [rbp - 1896], rax
    mov rax, [rbp - 1896]
    ; Store to variable: col
    mov r14d, eax       ; To register (int32/uint32)
ir_if_end_279:
    jmp ir_if_end_188
ir_if_next_278:
    cmp ebx, 62
    jne ir_if_next_291
    mov rax, 1
    mov r10, rax
    ; Load variable: pos
    movsxd rax, r13d      ; From register (signed int32)
    add rax, r10
    mov [rbp - 1912], rax
    test r15, r15
    jnz ir_nonnull_295
ir_trap_null_294:
    ; IR call: puts (1 args)
    sub rsp, 32
    ; String literal (37 bytes)
    lea rax, [rel Lstr_struct151]  ; Load string struct address
    mov rcx, rax
    call puts
    add rsp, 32
    ; Unknown return type - assuming integer
    ; IR call: exit (1 args)
    sub rsp, 32
    mov rax, 1
    mov rcx, rax
    call exit
    add rsp, 32
    ; Unknown return type - assuming integer
ir_nonnull_295:
    mov rax, 1
    mov r10, rax
    mov rax, [rbp - 1912]
    imul rax, r10
    mov [rbp - 1920], rax
    mov rax, [rbp - 1920]
    mov r10, rax
    ; Load variable: source
    mov rax, r15      ; From register
    add rax, r10
    mov [rbp - 1928], rax
    mov rax, [rbp - 1928]
    movzx rax, byte [rax]
    mov [rbp - 1936], rax
    mov rax, 61
    mov r10, rax
    mov rax, [rbp - 1936]
    cmp rax, r10
    jne ir_if_next_293
    test r12, r12
    jnz ir_nonnull_297
ir_trap_null_296:
    ; IR call: puts (1 args)
    sub rsp, 32
    ; String literal (37 bytes)
    lea rax, [rel Lstr_struct153]  ; Load string struct address
    mov rcx, rax
    call puts
    add rsp, 32
    ; Unknown return type - assuming integer
    ; IR call: exit (1 args)
    sub rsp, 32
    mov rax, 1
    mov rcx, rax
    call exit
    add rsp, 32
    ; Unknown return type - assuming integer
ir_nonnull_297:
    mov rax, 0
    mov r10, rax
    ; Load variable: tok
    mov rax, r12      ; From register
    add rax, r10
    mov [rbp - 1952], rax
    ; Load variable: TOKEN_GE
    movsxd rax, dword [rel TOKEN_GE]  ; From global memory
    mov rcx, rax
    mov rax, [rbp - 1952]
    mov dword [rax], ecx
    test r12, r12
    jnz ir_nonnull_299
ir_trap_null_298:
    ; IR call: puts (1 args)
    sub rsp, 32
    ; String literal (37 bytes)
    lea rax, [rel Lstr_struct155]  ; Load string struct address
    mov rcx, rax
    call puts
    add rsp, 32
    ; Unknown return type - assuming integer
    ; IR call: exit (1 args)
    sub rsp, 32
    mov rax, 1
    mov rcx, rax
    call exit
    add rsp, 32
    ; Unknown return type - assuming integer
ir_nonnull_299:
    mov rax, 8
    mov r10, rax
    ; Load variable: tok
    mov rax, r12      ; From register
    add rax, r10
    mov [rbp - 1968], rax
    mov rax, 2
    mov rcx, rax
    mov rax, [rbp - 1968]
    mov dword [rax], ecx
    mov rax, 2
    mov r10, rax
    ; Load variable: pos
    movsxd rax, r13d      ; From register (signed int32)
    add rax, r10
    mov [rbp - 1984], rax
    mov rax, [rbp - 1984]
    ; Store to variable: pos
    mov r13d, eax       ; To register (int32/uint32)
    mov rax, 2
    mov r10, rax
    ; Load variable: col
    movsxd rax, r14d      ; From register (signed int32)
    add rax, r10
    mov [rbp - 1992], rax
    mov rax, [rbp - 1992]
    ; Store to variable: col
    mov r14d, eax       ; To register (int32/uint32)
    jmp ir_if_end_292
ir_if_next_293:
    test r12, r12
    jnz ir_nonnull_301
ir_trap_null_300:
    ; IR call: puts (1 args)
    sub rsp, 32
    ; String literal (37 bytes)
    lea rax, [rel Lstr_struct157]  ; Load string struct address
    mov rcx, rax
    call puts
    add rsp, 32
    ; Unknown return type - assuming integer
    ; IR call: exit (1 args)
    sub rsp, 32
    mov rax, 1
    mov rcx, rax
    call exit
    add rsp, 32
    ; Unknown return type - assuming integer
ir_nonnull_301:
    mov rax, 0
    mov r10, rax
    ; Load variable: tok
    mov rax, r12      ; From register
    add rax, r10
    mov [rbp - 2000], rax
    ; Load variable: TOKEN_GT
    movsxd rax, dword [rel TOKEN_GT]  ; From global memory
    mov rcx, rax
    mov rax, [rbp - 2000]
    mov dword [rax], ecx
    test r12, r12
    jnz ir_nonnull_303
ir_trap_null_302:
    ; IR call: puts (1 args)
    sub rsp, 32
    ; String literal (37 bytes)
    lea rax, [rel Lstr_struct159]  ; Load string struct address
    mov rcx, rax
    call puts
    add rsp, 32
    ; Unknown return type - assuming integer
    ; IR call: exit (1 args)
    sub rsp, 32
    mov rax, 1
    mov rcx, rax
    call exit
    add rsp, 32
    ; Unknown return type - assuming integer
ir_nonnull_303:
    mov rax, 8
    mov r10, rax
    ; Load variable: tok
    mov rax, r12      ; From register
    add rax, r10
    mov [rbp - 2016], rax
    mov rax, 1
    mov rcx, rax
    mov rax, [rbp - 2016]
    mov dword [rax], ecx
    mov rax, 1
    mov r10, rax
    ; Load variable: pos
    movsxd rax, r13d      ; From register (signed int32)
    add rax, r10
    mov [rbp - 2032], rax
    mov rax, [rbp - 2032]
    ; Store to variable: pos
    mov r13d, eax       ; To register (int32/uint32)
    mov rax, 1
    mov r10, rax
    ; Load variable: col
    movsxd rax, r14d      ; From register (signed int32)
    add rax, r10
    mov [rbp - 2040], rax
    mov rax, [rbp - 2040]
    ; Store to variable: col
    mov r14d, eax       ; To register (int32/uint32)
ir_if_end_292:
    jmp ir_if_end_188
ir_if_next_291:
    cmp ebx, 38
    jne ir_if_next_304
    mov rax, 1
    mov r10, rax
    ; Load variable: pos
    movsxd rax, r13d      ; From register (signed int32)
    add rax, r10
    mov [rbp - 2056], rax
    test r15, r15
    jnz ir_nonnull_308
ir_trap_null_307:
    ; IR call: puts (1 args)
    sub rsp, 32
    ; String literal (37 bytes)
    lea rax, [rel Lstr_struct161]  ; Load string struct address
    mov rcx, rax
    call puts
    add rsp, 32
    ; Unknown return type - assuming integer
    ; IR call: exit (1 args)
    sub rsp, 32
    mov rax, 1
    mov rcx, rax
    call exit
    add rsp, 32
    ; Unknown return type - assuming integer
ir_nonnull_308:
    mov rax, 1
    mov r10, rax
    mov rax, [rbp - 2056]
    imul rax, r10
    mov [rbp - 2064], rax
    mov rax, [rbp - 2064]
    mov r10, rax
    ; Load variable: source
    mov rax, r15      ; From register
    add rax, r10
    mov [rbp - 2072], rax
    mov rax, [rbp - 2072]
    movzx rax, byte [rax]
    mov [rbp - 2080], rax
    mov rax, 38
    mov r10, rax
    mov rax, [rbp - 2080]
    cmp rax, r10
    jne ir_if_next_306
    test r12, r12
    jnz ir_nonnull_310
ir_trap_null_309:
    ; IR call: puts (1 args)
    sub rsp, 32
    ; String literal (37 bytes)
    lea rax, [rel Lstr_struct163]  ; Load string struct address
    mov rcx, rax
    call puts
    add rsp, 32
    ; Unknown return type - assuming integer
    ; IR call: exit (1 args)
    sub rsp, 32
    mov rax, 1
    mov rcx, rax
    call exit
    add rsp, 32
    ; Unknown return type - assuming integer
ir_nonnull_310:
    mov rax, 0
    mov r10, rax
    ; Load variable: tok
    mov rax, r12      ; From register
    add rax, r10
    mov [rbp - 2096], rax
    ; Load variable: TOKEN_AND
    movsxd rax, dword [rel TOKEN_AND]  ; From global memory
    mov rcx, rax
    mov rax, [rbp - 2096]
    mov dword [rax], ecx
    test r12, r12
    jnz ir_nonnull_312
ir_trap_null_311:
    ; IR call: puts (1 args)
    sub rsp, 32
    ; String literal (37 bytes)
    lea rax, [rel Lstr_struct165]  ; Load string struct address
    mov rcx, rax
    call puts
    add rsp, 32
    ; Unknown return type - assuming integer
    ; IR call: exit (1 args)
    sub rsp, 32
    mov rax, 1
    mov rcx, rax
    call exit
    add rsp, 32
    ; Unknown return type - assuming integer
ir_nonnull_312:
    mov rax, 8
    mov r10, rax
    ; Load variable: tok
    mov rax, r12      ; From register
    add rax, r10
    mov [rbp - 2112], rax
    mov rax, 2
    mov rcx, rax
    mov rax, [rbp - 2112]
    mov dword [rax], ecx
    mov rax, 2
    mov r10, rax
    ; Load variable: pos
    movsxd rax, r13d      ; From register (signed int32)
    add rax, r10
    mov [rbp - 2128], rax
    mov rax, [rbp - 2128]
    ; Store to variable: pos
    mov r13d, eax       ; To register (int32/uint32)
    mov rax, 2
    mov r10, rax
    ; Load variable: col
    movsxd rax, r14d      ; From register (signed int32)
    add rax, r10
    mov [rbp - 2136], rax
    mov rax, [rbp - 2136]
    ; Store to variable: col
    mov r14d, eax       ; To register (int32/uint32)
    jmp ir_if_end_305
ir_if_next_306:
    test r12, r12
    jnz ir_nonnull_314
ir_trap_null_313:
    ; IR call: puts (1 args)
    sub rsp, 32
    ; String literal (37 bytes)
    lea rax, [rel Lstr_struct167]  ; Load string struct address
    mov rcx, rax
    call puts
    add rsp, 32
    ; Unknown return type - assuming integer
    ; IR call: exit (1 args)
    sub rsp, 32
    mov rax, 1
    mov rcx, rax
    call exit
    add rsp, 32
    ; Unknown return type - assuming integer
ir_nonnull_314:
    mov rax, 0
    mov r10, rax
    ; Load variable: tok
    mov rax, r12      ; From register
    add rax, r10
    mov [rbp - 2144], rax
    ; Load variable: TOKEN_ERROR
    movsxd rax, dword [rel TOKEN_ERROR]  ; From global memory
    mov rcx, rax
    mov rax, [rbp - 2144]
    mov dword [rax], ecx
    test r12, r12
    jnz ir_nonnull_316
ir_trap_null_315:
    ; IR call: puts (1 args)
    sub rsp, 32
    ; String literal (37 bytes)
    lea rax, [rel Lstr_struct169]  ; Load string struct address
    mov rcx, rax
    call puts
    add rsp, 32
    ; Unknown return type - assuming integer
    ; IR call: exit (1 args)
    sub rsp, 32
    mov rax, 1
    mov rcx, rax
    call exit
    add rsp, 32
    ; Unknown return type - assuming integer
ir_nonnull_316:
    mov rax, 8
    mov r10, rax
    ; Load variable: tok
    mov rax, r12      ; From register
    add rax, r10
    mov [rbp - 2160], rax
    mov rax, 1
    mov rcx, rax
    mov rax, [rbp - 2160]
    mov dword [rax], ecx
    mov rax, 1
    neg rax
    mov [rbp - 2176], rax
    mov rax, [rbp - 2176]
    mov [rbp - 2184], rax
    cmp qword [rbp - 2184], 0
    jnz ir_errdefer_end_318
ir_errdefer_ok_317:
ir_errdefer_end_318:
    mov rax, [rbp - 2176]
    jmp Llex_all_exit
ir_if_end_305:
    jmp ir_if_end_188
ir_if_next_304:
    cmp ebx, 124
    jne ir_if_next_319
    mov rax, 1
    mov r10, rax
    ; Load variable: pos
    movsxd rax, r13d      ; From register (signed int32)
    add rax, r10
    mov [rbp - 2200], rax
    test r15, r15
    jnz ir_nonnull_323
ir_trap_null_322:
    ; IR call: puts (1 args)
    sub rsp, 32
    ; String literal (37 bytes)
    lea rax, [rel Lstr_struct171]  ; Load string struct address
    mov rcx, rax
    call puts
    add rsp, 32
    ; Unknown return type - assuming integer
    ; IR call: exit (1 args)
    sub rsp, 32
    mov rax, 1
    mov rcx, rax
    call exit
    add rsp, 32
    ; Unknown return type - assuming integer
ir_nonnull_323:
    mov rax, 1
    mov r10, rax
    mov rax, [rbp - 2200]
    imul rax, r10
    mov [rbp - 2208], rax
    mov rax, [rbp - 2208]
    mov r10, rax
    ; Load variable: source
    mov rax, r15      ; From register
    add rax, r10
    mov [rbp - 2216], rax
    mov rax, [rbp - 2216]
    movzx rax, byte [rax]
    mov [rbp - 2224], rax
    mov rax, 124
    mov r10, rax
    mov rax, [rbp - 2224]
    cmp rax, r10
    jne ir_if_next_321
    test r12, r12
    jnz ir_nonnull_325
ir_trap_null_324:
    ; IR call: puts (1 args)
    sub rsp, 32
    ; String literal (37 bytes)
    lea rax, [rel Lstr_struct173]  ; Load string struct address
    mov rcx, rax
    call puts
    add rsp, 32
    ; Unknown return type - assuming integer
    ; IR call: exit (1 args)
    sub rsp, 32
    mov rax, 1
    mov rcx, rax
    call exit
    add rsp, 32
    ; Unknown return type - assuming integer
ir_nonnull_325:
    mov rax, 0
    mov r10, rax
    ; Load variable: tok
    mov rax, r12      ; From register
    add rax, r10
    mov [rbp - 2240], rax
    ; Load variable: TOKEN_OR
    movsxd rax, dword [rel TOKEN_OR]  ; From global memory
    mov rcx, rax
    mov rax, [rbp - 2240]
    mov dword [rax], ecx
    test r12, r12
    jnz ir_nonnull_327
ir_trap_null_326:
    ; IR call: puts (1 args)
    sub rsp, 32
    ; String literal (37 bytes)
    lea rax, [rel Lstr_struct175]  ; Load string struct address
    mov rcx, rax
    call puts
    add rsp, 32
    ; Unknown return type - assuming integer
    ; IR call: exit (1 args)
    sub rsp, 32
    mov rax, 1
    mov rcx, rax
    call exit
    add rsp, 32
    ; Unknown return type - assuming integer
ir_nonnull_327:
    mov rax, 8
    mov r10, rax
    ; Load variable: tok
    mov rax, r12      ; From register
    add rax, r10
    mov [rbp - 2256], rax
    mov rax, 2
    mov rcx, rax
    mov rax, [rbp - 2256]
    mov dword [rax], ecx
    mov rax, 2
    mov r10, rax
    ; Load variable: pos
    movsxd rax, r13d      ; From register (signed int32)
    add rax, r10
    mov [rbp - 2272], rax
    mov rax, [rbp - 2272]
    ; Store to variable: pos
    mov r13d, eax       ; To register (int32/uint32)
    mov rax, 2
    mov r10, rax
    ; Load variable: col
    movsxd rax, r14d      ; From register (signed int32)
    add rax, r10
    mov [rbp - 2280], rax
    mov rax, [rbp - 2280]
    ; Store to variable: col
    mov r14d, eax       ; To register (int32/uint32)
    jmp ir_if_end_320
ir_if_next_321:
    test r12, r12
    jnz ir_nonnull_329
ir_trap_null_328:
    ; IR call: puts (1 args)
    sub rsp, 32
    ; String literal (37 bytes)
    lea rax, [rel Lstr_struct177]  ; Load string struct address
    mov rcx, rax
    call puts
    add rsp, 32
    ; Unknown return type - assuming integer
    ; IR call: exit (1 args)
    sub rsp, 32
    mov rax, 1
    mov rcx, rax
    call exit
    add rsp, 32
    ; Unknown return type - assuming integer
ir_nonnull_329:
    mov rax, 0
    mov r10, rax
    ; Load variable: tok
    mov rax, r12      ; From register
    add rax, r10
    mov [rbp - 2288], rax
    ; Load variable: TOKEN_ERROR
    movsxd rax, dword [rel TOKEN_ERROR]  ; From global memory
    mov rcx, rax
    mov rax, [rbp - 2288]
    mov dword [rax], ecx
    test r12, r12
    jnz ir_nonnull_331
ir_trap_null_330:
    ; IR call: puts (1 args)
    sub rsp, 32
    ; String literal (37 bytes)
    lea rax, [rel Lstr_struct179]  ; Load string struct address
    mov rcx, rax
    call puts
    add rsp, 32
    ; Unknown return type - assuming integer
    ; IR call: exit (1 args)
    sub rsp, 32
    mov rax, 1
    mov rcx, rax
    call exit
    add rsp, 32
    ; Unknown return type - assuming integer
ir_nonnull_331:
    mov rax, 8
    mov r10, rax
    ; Load variable: tok
    mov rax, r12      ; From register
    add rax, r10
    mov [rbp - 2304], rax
    mov rax, 1
    mov rcx, rax
    mov rax, [rbp - 2304]
    mov dword [rax], ecx
    mov rax, 1
    neg rax
    mov [rbp - 2320], rax
    mov rax, [rbp - 2320]
    mov [rbp - 2328], rax
    cmp qword [rbp - 2328], 0
    jnz ir_errdefer_end_333
ir_errdefer_ok_332:
ir_errdefer_end_333:
    mov rax, [rbp - 2320]
    jmp Llex_all_exit
ir_if_end_320:
    jmp ir_if_end_188
ir_if_next_319:
    cmp ebx, 34
    jne ir_if_next_334
    ; Load variable: pos
    movsxd rax, r13d      ; From register (signed int32)
    ; Store to variable: str_start
    mov dword [rbp - 76], eax  ; To stack [rbp - 76]
    mov rax, 1
    mov r10, rax
    ; Load variable: pos
    movsxd rax, r13d      ; From register (signed int32)
    add rax, r10
    mov [rbp - 2344], rax
    mov rax, [rbp - 2344]
    ; Store to variable: pos
    mov r13d, eax       ; To register (int32/uint32)
    mov rax, 1
    mov r10, rax
    ; Load variable: col
    movsxd rax, r14d      ; From register (signed int32)
    add rax, r10
    mov [rbp - 2352], rax
    mov rax, [rbp - 2352]
    ; Store to variable: col
    mov r14d, eax       ; To register (int32/uint32)
ir_while_335:
    test r15, r15
    jnz ir_nonnull_338
ir_trap_null_337:
    ; IR call: puts (1 args)
    sub rsp, 32
    ; String literal (37 bytes)
    lea rax, [rel Lstr_struct181]  ; Load string struct address
    mov rcx, rax
    call puts
    add rsp, 32
    ; Unknown return type - assuming integer
    ; IR call: exit (1 args)
    sub rsp, 32
    mov rax, 1
    mov rcx, rax
    call exit
    add rsp, 32
    ; Unknown return type - assuming integer
ir_nonnull_338:
    mov rax, 1
    mov r10, rax
    ; Load variable: pos
    movsxd rax, r13d      ; From register (signed int32)
    imul rax, r10
    mov [rbp - 2360], rax
    mov rax, [rbp - 2360]
    mov r10, rax
    ; Load variable: source
    mov rax, r15      ; From register
    add rax, r10
    mov [rbp - 2368], rax
    mov rax, [rbp - 2368]
    movzx rax, byte [rax]
    mov [rbp - 2376], rax
    mov rax, 0
    mov r10, rax
    mov rax, [rbp - 2376]
    cmp rax, r10
    je ir_while_end_336
    test r15, r15
    jnz ir_nonnull_342
ir_trap_null_341:
    ; IR call: puts (1 args)
    sub rsp, 32
    ; String literal (37 bytes)
    lea rax, [rel Lstr_struct183]  ; Load string struct address
    mov rcx, rax
    call puts
    add rsp, 32
    ; Unknown return type - assuming integer
    ; IR call: exit (1 args)
    sub rsp, 32
    mov rax, 1
    mov rcx, rax
    call exit
    add rsp, 32
    ; Unknown return type - assuming integer
ir_nonnull_342:
    mov rax, 1
    mov r10, rax
    ; Load variable: pos
    movsxd rax, r13d      ; From register (signed int32)
    imul rax, r10
    mov [rbp - 2392], rax
    mov rax, [rbp - 2392]
    mov r10, rax
    ; Load variable: source
    mov rax, r15      ; From register
    add rax, r10
    mov [rbp - 2400], rax
    mov rax, [rbp - 2400]
    movzx rax, byte [rax]
    mov [rbp - 2408], rax
    mov rax, 34
    mov r10, rax
    mov rax, [rbp - 2408]
    cmp rax, r10
    jne ir_if_next_340
    mov rax, 1
    mov r10, rax
    ; Load variable: pos
    movsxd rax, r13d      ; From register (signed int32)
    add rax, r10
    mov [rbp - 2424], rax
    mov rax, [rbp - 2424]
    ; Store to variable: pos
    mov r13d, eax       ; To register (int32/uint32)
    mov rax, 1
    mov r10, rax
    ; Load variable: col
    movsxd rax, r14d      ; From register (signed int32)
    add rax, r10
    mov [rbp - 2432], rax
    mov rax, [rbp - 2432]
    ; Store to variable: col
    mov r14d, eax       ; To register (int32/uint32)
    test r12, r12
    jnz ir_nonnull_344
ir_trap_null_343:
    ; IR call: puts (1 args)
    sub rsp, 32
    ; String literal (37 bytes)
    lea rax, [rel Lstr_struct185]  ; Load string struct address
    mov rcx, rax
    call puts
    add rsp, 32
    ; Unknown return type - assuming integer
    ; IR call: exit (1 args)
    sub rsp, 32
    mov rax, 1
    mov rcx, rax
    call exit
    add rsp, 32
    ; Unknown return type - assuming integer
ir_nonnull_344:
    mov rax, 0
    mov r10, rax
    ; Load variable: tok
    mov rax, r12      ; From register
    add rax, r10
    mov [rbp - 2440], rax
    ; Load variable: TOKEN_STRING
    movsxd rax, dword [rel TOKEN_STRING]  ; From global memory
    mov rcx, rax
    mov rax, [rbp - 2440]
    mov dword [rax], ecx
    mov rax, 1
    mov r10, rax
    ; Load variable: str_start
    movsxd rax, dword [rbp - 76]  ; From stack [rbp - 76]
    add rax, r10
    mov [rbp - 2456], rax
    test r12, r12
    jnz ir_nonnull_346
ir_trap_null_345:
    ; IR call: puts (1 args)
    sub rsp, 32
    ; String literal (37 bytes)
    lea rax, [rel Lstr_struct187]  ; Load string struct address
    mov rcx, rax
    call puts
    add rsp, 32
    ; Unknown return type - assuming integer
    ; IR call: exit (1 args)
    sub rsp, 32
    mov rax, 1
    mov rcx, rax
    call exit
    add rsp, 32
    ; Unknown return type - assuming integer
ir_nonnull_346:
    mov rax, 4
    mov r10, rax
    ; Load variable: tok
    mov rax, r12      ; From register
    add rax, r10
    mov [rbp - 2464], rax
    mov rax, [rbp - 2456]
    mov rcx, rax
    mov rax, [rbp - 2464]
    mov dword [rax], ecx
    mov rax, 1
    mov r10, rax
    ; Load variable: pos
    movsxd rax, r13d      ; From register (signed int32)
    sub rax, r10
    mov [rbp - 2480], rax
    mov rax, 1
    mov r10, rax
    ; Load variable: str_start
    movsxd rax, dword [rbp - 76]  ; From stack [rbp - 76]
    add rax, r10
    mov [rbp - 2488], rax
    mov rax, [rbp - 2488]
    mov r10, rax
    mov rax, [rbp - 2480]
    sub rax, r10
    mov [rbp - 2496], rax
    test r12, r12
    jnz ir_nonnull_348
ir_trap_null_347:
    ; IR call: puts (1 args)
    sub rsp, 32
    ; String literal (37 bytes)
    lea rax, [rel Lstr_struct189]  ; Load string struct address
    mov rcx, rax
    call puts
    add rsp, 32
    ; Unknown return type - assuming integer
    ; IR call: exit (1 args)
    sub rsp, 32
    mov rax, 1
    mov rcx, rax
    call exit
    add rsp, 32
    ; Unknown return type - assuming integer
ir_nonnull_348:
    mov rax, 8
    mov r10, rax
    ; Load variable: tok
    mov rax, r12      ; From register
    add rax, r10
    mov [rbp - 2504], rax
    mov rax, [rbp - 2496]
    mov rcx, rax
    mov rax, [rbp - 2504]
    mov dword [rax], ecx
    jmp ir_while_end_336
    jmp ir_if_end_339
ir_if_next_340:
ir_if_end_339:
    test r15, r15
    jnz ir_nonnull_352
ir_trap_null_351:
    ; IR call: puts (1 args)
    sub rsp, 32
    ; String literal (37 bytes)
    lea rax, [rel Lstr_struct191]  ; Load string struct address
    mov rcx, rax
    call puts
    add rsp, 32
    ; Unknown return type - assuming integer
    ; IR call: exit (1 args)
    sub rsp, 32
    mov rax, 1
    mov rcx, rax
    call exit
    add rsp, 32
    ; Unknown return type - assuming integer
ir_nonnull_352:
    mov rax, 1
    mov r10, rax
    ; Load variable: pos
    movsxd rax, r13d      ; From register (signed int32)
    imul rax, r10
    mov [rbp - 2520], rax
    mov rax, [rbp - 2520]
    mov r10, rax
    ; Load variable: source
    mov rax, r15      ; From register
    add rax, r10
    mov [rbp - 2528], rax
    mov rax, [rbp - 2528]
    movzx rax, byte [rax]
    mov [rbp - 2536], rax
    mov rax, 92
    mov r10, rax
    mov rax, [rbp - 2536]
    cmp rax, r10
    jne ir_if_next_350
    mov rax, 2
    mov r10, rax
    ; Load variable: pos
    movsxd rax, r13d      ; From register (signed int32)
    add rax, r10
    mov [rbp - 2552], rax
    mov rax, [rbp - 2552]
    ; Store to variable: pos
    mov r13d, eax       ; To register (int32/uint32)
    mov rax, 2
    mov r10, rax
    ; Load variable: col
    movsxd rax, r14d      ; From register (signed int32)
    add rax, r10
    mov [rbp - 2560], rax
    mov rax, [rbp - 2560]
    ; Store to variable: col
    mov r14d, eax       ; To register (int32/uint32)
    jmp ir_if_end_349
ir_if_next_350:
    mov rax, 1
    mov r10, rax
    ; Load variable: pos
    movsxd rax, r13d      ; From register (signed int32)
    add rax, r10
    mov [rbp - 2568], rax
    mov rax, [rbp - 2568]
    ; Store to variable: pos
    mov r13d, eax       ; To register (int32/uint32)
    mov rax, 1
    mov r10, rax
    ; Load variable: col
    movsxd rax, r14d      ; From register (signed int32)
    add rax, r10
    mov [rbp - 2576], rax
    mov rax, [rbp - 2576]
    ; Store to variable: col
    mov r14d, eax       ; To register (int32/uint32)
ir_if_end_349:
    jmp ir_while_335
ir_while_end_336:
    test r12, r12
    jnz ir_nonnull_356
ir_trap_null_355:
    ; IR call: puts (1 args)
    sub rsp, 32
    ; String literal (37 bytes)
    lea rax, [rel Lstr_struct193]  ; Load string struct address
    mov rcx, rax
    call puts
    add rsp, 32
    ; Unknown return type - assuming integer
    ; IR call: exit (1 args)
    sub rsp, 32
    mov rax, 1
    mov rcx, rax
    call exit
    add rsp, 32
    ; Unknown return type - assuming integer
ir_nonnull_356:
    mov rax, 0
    mov r10, rax
    ; Load variable: tok
    mov rax, r12      ; From register
    add rax, r10
    mov [rbp - 2584], rax
    mov rax, [rbp - 2584]
    mov eax, dword [rax]
    mov [rbp - 2592], rax
    ; Load variable: TOKEN_STRING
    movsxd rax, dword [rel TOKEN_STRING]  ; From global memory
    mov r10, rax
    mov rax, [rbp - 2592]
    cmp rax, r10
    je ir_if_next_354
    test r12, r12
    jnz ir_nonnull_358
ir_trap_null_357:
    ; IR call: puts (1 args)
    sub rsp, 32
    ; String literal (37 bytes)
    lea rax, [rel Lstr_struct195]  ; Load string struct address
    mov rcx, rax
    call puts
    add rsp, 32
    ; Unknown return type - assuming integer
    ; IR call: exit (1 args)
    sub rsp, 32
    mov rax, 1
    mov rcx, rax
    call exit
    add rsp, 32
    ; Unknown return type - assuming integer
ir_nonnull_358:
    mov rax, 0
    mov r10, rax
    ; Load variable: tok
    mov rax, r12      ; From register
    add rax, r10
    mov [rbp - 2608], rax
    ; Load variable: TOKEN_ERROR
    movsxd rax, dword [rel TOKEN_ERROR]  ; From global memory
    mov rcx, rax
    mov rax, [rbp - 2608]
    mov dword [rax], ecx
    mov rax, 1
    neg rax
    mov [rbp - 2624], rax
    mov rax, [rbp - 2624]
    mov [rbp - 2632], rax
    cmp qword [rbp - 2632], 0
    jnz ir_errdefer_end_360
ir_errdefer_ok_359:
ir_errdefer_end_360:
    mov rax, [rbp - 2624]
    jmp Llex_all_exit
    jmp ir_if_end_353
ir_if_next_354:
ir_if_end_353:
    jmp ir_if_end_188
ir_if_next_334:
    ; IR call: is_alpha (1 args)
    ; Load variable: c
    movsxd rax, ebx      ; From register (signed int32)
    mov rcx, rax
    call is_alpha
    ; Integer/pointer return value in rax
    ; 32-bit return value already in eax
    mov [rbp - 2640], rax
    mov rax, 0
    mov r10, rax
    mov rax, [rbp - 2640]
    cmp rax, r10
    je ir_if_next_361
    ; Load variable: pos
    movsxd rax, r13d      ; From register (signed int32)
    ; Store to variable: start
    mov dword [rbp - 80], eax  ; To stack [rbp - 80]
    mov rax, 1
    mov r10, rax
    ; Load variable: pos
    movsxd rax, r13d      ; From register (signed int32)
    add rax, r10
    mov [rbp - 2656], rax
    mov rax, [rbp - 2656]
    ; Store to variable: pos
    mov r13d, eax       ; To register (int32/uint32)
    mov rax, 1
    mov r10, rax
    ; Load variable: col
    movsxd rax, r14d      ; From register (signed int32)
    add rax, r10
    mov [rbp - 2664], rax
    mov rax, [rbp - 2664]
    ; Store to variable: col
    mov r14d, eax       ; To register (int32/uint32)
ir_while_362:
    test r15, r15
    jnz ir_nonnull_365
ir_trap_null_364:
    ; IR call: puts (1 args)
    sub rsp, 32
    ; String literal (37 bytes)
    lea rax, [rel Lstr_struct197]  ; Load string struct address
    mov rcx, rax
    call puts
    add rsp, 32
    ; Unknown return type - assuming integer
    ; IR call: exit (1 args)
    sub rsp, 32
    mov rax, 1
    mov rcx, rax
    call exit
    add rsp, 32
    ; Unknown return type - assuming integer
ir_nonnull_365:
    mov rax, 1
    mov r10, rax
    ; Load variable: pos
    movsxd rax, r13d      ; From register (signed int32)
    imul rax, r10
    mov [rbp - 2672], rax
    mov rax, [rbp - 2672]
    mov r10, rax
    ; Load variable: source
    mov rax, r15      ; From register
    add rax, r10
    mov [rbp - 2680], rax
    mov rax, [rbp - 2680]
    movzx rax, byte [rax]
    mov [rbp - 2688], rax
    ; IR call: is_alnum (1 args)
    mov rax, [rbp - 2688]
    mov rcx, rax
    call is_alnum
    ; Integer/pointer return value in rax
    ; 32-bit return value already in eax
    mov [rbp - 2696], rax
    mov rax, 0
    mov r10, rax
    mov rax, [rbp - 2696]
    cmp rax, r10
    je ir_while_end_363
    mov rax, 1
    mov r10, rax
    ; Load variable: pos
    movsxd rax, r13d      ; From register (signed int32)
    add rax, r10
    mov [rbp - 2712], rax
    mov rax, [rbp - 2712]
    ; Store to variable: pos
    mov r13d, eax       ; To register (int32/uint32)
    mov rax, 1
    mov r10, rax
    ; Load variable: col
    movsxd rax, r14d      ; From register (signed int32)
    add rax, r10
    mov [rbp - 2720], rax
    mov rax, [rbp - 2720]
    ; Store to variable: col
    mov r14d, eax       ; To register (int32/uint32)
    jmp ir_while_362
ir_while_end_363:
    ; Load variable: start
    movsxd rax, dword [rbp - 80]  ; From stack [rbp - 80]
    mov r10, rax
    ; Load variable: pos
    movsxd rax, r13d      ; From register (signed int32)
    sub rax, r10
    mov [rbp - 2728], rax
    ; IR call: keyword_type (3 args)
    ; Load variable: source
    mov rax, r15      ; From register
    mov rcx, rax
    ; Load variable: start
    movsxd rax, dword [rbp - 80]  ; From stack [rbp - 80]
    mov rdx, rax
    mov rax, [rbp - 2728]
    mov r8, rax
    call keyword_type
    ; Integer/pointer return value in rax
    ; 32-bit return value already in eax
    mov [rbp - 2736], rax
    test r12, r12
    jnz ir_nonnull_367
ir_trap_null_366:
    ; IR call: puts (1 args)
    sub rsp, 32
    ; String literal (37 bytes)
    lea rax, [rel Lstr_struct199]  ; Load string struct address
    mov rcx, rax
    call puts
    add rsp, 32
    ; Unknown return type - assuming integer
    ; IR call: exit (1 args)
    sub rsp, 32
    mov rax, 1
    mov rcx, rax
    call exit
    add rsp, 32
    ; Unknown return type - assuming integer
ir_nonnull_367:
    mov rax, 0
    mov r10, rax
    ; Load variable: tok
    mov rax, r12      ; From register
    add rax, r10
    mov [rbp - 2744], rax
    mov rax, [rbp - 2736]
    mov rcx, rax
    mov rax, [rbp - 2744]
    mov dword [rax], ecx
    test r12, r12
    jnz ir_nonnull_369
ir_trap_null_368:
    ; IR call: puts (1 args)
    sub rsp, 32
    ; String literal (37 bytes)
    lea rax, [rel Lstr_struct201]  ; Load string struct address
    mov rcx, rax
    call puts
    add rsp, 32
    ; Unknown return type - assuming integer
    ; IR call: exit (1 args)
    sub rsp, 32
    mov rax, 1
    mov rcx, rax
    call exit
    add rsp, 32
    ; Unknown return type - assuming integer
ir_nonnull_369:
    mov rax, 4
    mov r10, rax
    ; Load variable: tok
    mov rax, r12      ; From register
    add rax, r10
    mov [rbp - 2760], rax
    ; Load variable: start
    movsxd rax, dword [rbp - 80]  ; From stack [rbp - 80]
    mov rcx, rax
    mov rax, [rbp - 2760]
    mov dword [rax], ecx
    ; Load variable: start
    movsxd rax, dword [rbp - 80]  ; From stack [rbp - 80]
    mov r10, rax
    ; Load variable: pos
    movsxd rax, r13d      ; From register (signed int32)
    sub rax, r10
    mov [rbp - 2776], rax
    test r12, r12
    jnz ir_nonnull_371
ir_trap_null_370:
    ; IR call: puts (1 args)
    sub rsp, 32
    ; String literal (37 bytes)
    lea rax, [rel Lstr_struct203]  ; Load string struct address
    mov rcx, rax
    call puts
    add rsp, 32
    ; Unknown return type - assuming integer
    ; IR call: exit (1 args)
    sub rsp, 32
    mov rax, 1
    mov rcx, rax
    call exit
    add rsp, 32
    ; Unknown return type - assuming integer
ir_nonnull_371:
    mov rax, 8
    mov r10, rax
    ; Load variable: tok
    mov rax, r12      ; From register
    add rax, r10
    mov [rbp - 2784], rax
    mov rax, [rbp - 2776]
    mov rcx, rax
    mov rax, [rbp - 2784]
    mov dword [rax], ecx
    jmp ir_if_end_188
ir_if_next_361:
    ; IR call: is_digit (1 args)
    ; Load variable: c
    movsxd rax, ebx      ; From register (signed int32)
    mov rcx, rax
    call is_digit
    ; Integer/pointer return value in rax
    ; 32-bit return value already in eax
    mov [rbp - 2800], rax
    mov rax, 0
    mov r10, rax
    mov rax, [rbp - 2800]
    cmp rax, r10
    je ir_if_next_372
    ; Load variable: pos
    movsxd rax, r13d      ; From register (signed int32)
    ; Store to variable: start
    mov dword [rbp - 80], eax  ; To stack [rbp - 80]
    mov rax, 1
    mov r10, rax
    ; Load variable: pos
    movsxd rax, r13d      ; From register (signed int32)
    add rax, r10
    mov [rbp - 2816], rax
    mov rax, [rbp - 2816]
    ; Store to variable: pos
    mov r13d, eax       ; To register (int32/uint32)
    mov rax, 1
    mov r10, rax
    ; Load variable: col
    movsxd rax, r14d      ; From register (signed int32)
    add rax, r10
    mov [rbp - 2824], rax
    mov rax, [rbp - 2824]
    ; Store to variable: col
    mov r14d, eax       ; To register (int32/uint32)
ir_while_373:
    test r15, r15
    jnz ir_nonnull_376
ir_trap_null_375:
    ; IR call: puts (1 args)
    sub rsp, 32
    ; String literal (37 bytes)
    lea rax, [rel Lstr_struct205]  ; Load string struct address
    mov rcx, rax
    call puts
    add rsp, 32
    ; Unknown return type - assuming integer
    ; IR call: exit (1 args)
    sub rsp, 32
    mov rax, 1
    mov rcx, rax
    call exit
    add rsp, 32
    ; Unknown return type - assuming integer
ir_nonnull_376:
    mov rax, 1
    mov r10, rax
    ; Load variable: pos
    movsxd rax, r13d      ; From register (signed int32)
    imul rax, r10
    mov [rbp - 2832], rax
    mov rax, [rbp - 2832]
    mov r10, rax
    ; Load variable: source
    mov rax, r15      ; From register
    add rax, r10
    mov [rbp - 2840], rax
    mov rax, [rbp - 2840]
    movzx rax, byte [rax]
    mov [rbp - 2848], rax
    ; IR call: is_digit (1 args)
    mov rax, [rbp - 2848]
    mov rcx, rax
    call is_digit
    ; Integer/pointer return value in rax
    ; 32-bit return value already in eax
    mov [rbp - 2856], rax
    mov rax, 0
    mov r10, rax
    mov rax, [rbp - 2856]
    cmp rax, r10
    je ir_while_end_374
    mov rax, 1
    mov r10, rax
    ; Load variable: pos
    movsxd rax, r13d      ; From register (signed int32)
    add rax, r10
    mov [rbp - 2872], rax
    mov rax, [rbp - 2872]
    ; Store to variable: pos
    mov r13d, eax       ; To register (int32/uint32)
    mov rax, 1
    mov r10, rax
    ; Load variable: col
    movsxd rax, r14d      ; From register (signed int32)
    add rax, r10
    mov [rbp - 2880], rax
    mov rax, [rbp - 2880]
    ; Store to variable: col
    mov r14d, eax       ; To register (int32/uint32)
    jmp ir_while_373
ir_while_end_374:
    test r15, r15
    jnz ir_nonnull_380
ir_trap_null_379:
    ; IR call: puts (1 args)
    sub rsp, 32
    ; String literal (37 bytes)
    lea rax, [rel Lstr_struct207]  ; Load string struct address
    mov rcx, rax
    call puts
    add rsp, 32
    ; Unknown return type - assuming integer
    ; IR call: exit (1 args)
    sub rsp, 32
    mov rax, 1
    mov rcx, rax
    call exit
    add rsp, 32
    ; Unknown return type - assuming integer
ir_nonnull_380:
    mov rax, 1
    mov r10, rax
    ; Load variable: pos
    movsxd rax, r13d      ; From register (signed int32)
    imul rax, r10
    mov [rbp - 2888], rax
    mov rax, [rbp - 2888]
    mov r10, rax
    ; Load variable: source
    mov rax, r15      ; From register
    add rax, r10
    mov [rbp - 2896], rax
    mov rax, [rbp - 2896]
    movzx rax, byte [rax]
    mov [rbp - 2904], rax
    mov rax, 46
    mov r10, rax
    mov rax, [rbp - 2904]
    cmp rax, r10
    jne ir_if_next_378
    mov rax, 1
    mov r10, rax
    ; Load variable: pos
    movsxd rax, r13d      ; From register (signed int32)
    add rax, r10
    mov [rbp - 2920], rax
    mov rax, [rbp - 2920]
    ; Store to variable: pos
    mov r13d, eax       ; To register (int32/uint32)
    mov rax, 1
    mov r10, rax
    ; Load variable: col
    movsxd rax, r14d      ; From register (signed int32)
    add rax, r10
    mov [rbp - 2928], rax
    mov rax, [rbp - 2928]
    ; Store to variable: col
    mov r14d, eax       ; To register (int32/uint32)
ir_while_381:
    test r15, r15
    jnz ir_nonnull_384
ir_trap_null_383:
    ; IR call: puts (1 args)
    sub rsp, 32
    ; String literal (37 bytes)
    lea rax, [rel Lstr_struct209]  ; Load string struct address
    mov rcx, rax
    call puts
    add rsp, 32
    ; Unknown return type - assuming integer
    ; IR call: exit (1 args)
    sub rsp, 32
    mov rax, 1
    mov rcx, rax
    call exit
    add rsp, 32
    ; Unknown return type - assuming integer
ir_nonnull_384:
    mov rax, 1
    mov r10, rax
    ; Load variable: pos
    movsxd rax, r13d      ; From register (signed int32)
    imul rax, r10
    mov [rbp - 2936], rax
    mov rax, [rbp - 2936]
    mov r10, rax
    ; Load variable: source
    mov rax, r15      ; From register
    add rax, r10
    mov [rbp - 2944], rax
    mov rax, [rbp - 2944]
    movzx rax, byte [rax]
    mov [rbp - 2952], rax
    ; IR call: is_digit (1 args)
    mov rax, [rbp - 2952]
    mov rcx, rax
    call is_digit
    ; Integer/pointer return value in rax
    ; 32-bit return value already in eax
    mov [rbp - 2960], rax
    mov rax, 0
    mov r10, rax
    mov rax, [rbp - 2960]
    cmp rax, r10
    je ir_while_end_382
    mov rax, 1
    mov r10, rax
    ; Load variable: pos
    movsxd rax, r13d      ; From register (signed int32)
    add rax, r10
    mov [rbp - 2976], rax
    mov rax, [rbp - 2976]
    ; Store to variable: pos
    mov r13d, eax       ; To register (int32/uint32)
    mov rax, 1
    mov r10, rax
    ; Load variable: col
    movsxd rax, r14d      ; From register (signed int32)
    add rax, r10
    mov [rbp - 2984], rax
    mov rax, [rbp - 2984]
    ; Store to variable: col
    mov r14d, eax       ; To register (int32/uint32)
    jmp ir_while_381
ir_while_end_382:
    jmp ir_if_end_377
ir_if_next_378:
ir_if_end_377:
    test r15, r15
    jnz ir_nonnull_388
ir_trap_null_387:
    ; IR call: puts (1 args)
    sub rsp, 32
    ; String literal (37 bytes)
    lea rax, [rel Lstr_struct211]  ; Load string struct address
    mov rcx, rax
    call puts
    add rsp, 32
    ; Unknown return type - assuming integer
    ; IR call: exit (1 args)
    sub rsp, 32
    mov rax, 1
    mov rcx, rax
    call exit
    add rsp, 32
    ; Unknown return type - assuming integer
ir_nonnull_388:
    mov rax, 1
    mov r10, rax
    ; Load variable: pos
    movsxd rax, r13d      ; From register (signed int32)
    imul rax, r10
    mov [rbp - 2992], rax
    mov rax, [rbp - 2992]
    mov r10, rax
    ; Load variable: source
    mov rax, r15      ; From register
    add rax, r10
    mov [rbp - 3000], rax
    mov rax, [rbp - 3000]
    movzx rax, byte [rax]
    mov [rbp - 3008], rax
    mov rax, 101
    mov r10, rax
    mov rax, [rbp - 3008]
    cmp rax, r10
    jne ir_if_next_386
    mov rax, 1
    mov r10, rax
    ; Load variable: pos
    movsxd rax, r13d      ; From register (signed int32)
    add rax, r10
    mov [rbp - 3024], rax
    mov rax, [rbp - 3024]
    ; Store to variable: pos
    mov r13d, eax       ; To register (int32/uint32)
    mov rax, 1
    mov r10, rax
    ; Load variable: col
    movsxd rax, r14d      ; From register (signed int32)
    add rax, r10
    mov [rbp - 3032], rax
    mov rax, [rbp - 3032]
    ; Store to variable: col
    mov r14d, eax       ; To register (int32/uint32)
    test r15, r15
    jnz ir_nonnull_392
ir_trap_null_391:
    ; IR call: puts (1 args)
    sub rsp, 32
    ; String literal (37 bytes)
    lea rax, [rel Lstr_struct213]  ; Load string struct address
    mov rcx, rax
    call puts
    add rsp, 32
    ; Unknown return type - assuming integer
    ; IR call: exit (1 args)
    sub rsp, 32
    mov rax, 1
    mov rcx, rax
    call exit
    add rsp, 32
    ; Unknown return type - assuming integer
ir_nonnull_392:
    mov rax, 1
    mov r10, rax
    ; Load variable: pos
    movsxd rax, r13d      ; From register (signed int32)
    imul rax, r10
    mov [rbp - 3040], rax
    mov rax, [rbp - 3040]
    mov r10, rax
    ; Load variable: source
    mov rax, r15      ; From register
    add rax, r10
    mov [rbp - 3048], rax
    mov rax, [rbp - 3048]
    movzx rax, byte [rax]
    mov [rbp - 3056], rax
    mov rax, 43
    mov r10, rax
    mov rax, [rbp - 3056]
    cmp rax, r10
    jne ir_if_next_390
    mov rax, 1
    mov r10, rax
    ; Load variable: pos
    movsxd rax, r13d      ; From register (signed int32)
    add rax, r10
    mov [rbp - 3072], rax
    mov rax, [rbp - 3072]
    ; Store to variable: pos
    mov r13d, eax       ; To register (int32/uint32)
    mov rax, 1
    mov r10, rax
    ; Load variable: col
    movsxd rax, r14d      ; From register (signed int32)
    add rax, r10
    mov [rbp - 3080], rax
    mov rax, [rbp - 3080]
    ; Store to variable: col
    mov r14d, eax       ; To register (int32/uint32)
    jmp ir_if_end_389
ir_if_next_390:
ir_if_end_389:
    test r15, r15
    jnz ir_nonnull_396
ir_trap_null_395:
    ; IR call: puts (1 args)
    sub rsp, 32
    ; String literal (37 bytes)
    lea rax, [rel Lstr_struct215]  ; Load string struct address
    mov rcx, rax
    call puts
    add rsp, 32
    ; Unknown return type - assuming integer
    ; IR call: exit (1 args)
    sub rsp, 32
    mov rax, 1
    mov rcx, rax
    call exit
    add rsp, 32
    ; Unknown return type - assuming integer
ir_nonnull_396:
    mov rax, 1
    mov r10, rax
    ; Load variable: pos
    movsxd rax, r13d      ; From register (signed int32)
    imul rax, r10
    mov [rbp - 3088], rax
    mov rax, [rbp - 3088]
    mov r10, rax
    ; Load variable: source
    mov rax, r15      ; From register
    add rax, r10
    mov [rbp - 3096], rax
    mov rax, [rbp - 3096]
    movzx rax, byte [rax]
    mov [rbp - 3104], rax
    mov rax, 45
    mov r10, rax
    mov rax, [rbp - 3104]
    cmp rax, r10
    jne ir_if_next_394
    mov rax, 1
    mov r10, rax
    ; Load variable: pos
    movsxd rax, r13d      ; From register (signed int32)
    add rax, r10
    mov [rbp - 3120], rax
    mov rax, [rbp - 3120]
    ; Store to variable: pos
    mov r13d, eax       ; To register (int32/uint32)
    mov rax, 1
    mov r10, rax
    ; Load variable: col
    movsxd rax, r14d      ; From register (signed int32)
    add rax, r10
    mov [rbp - 3128], rax
    mov rax, [rbp - 3128]
    ; Store to variable: col
    mov r14d, eax       ; To register (int32/uint32)
    jmp ir_if_end_393
ir_if_next_394:
ir_if_end_393:
ir_while_397:
    test r15, r15
    jnz ir_nonnull_400
ir_trap_null_399:
    ; IR call: puts (1 args)
    sub rsp, 32
    ; String literal (37 bytes)
    lea rax, [rel Lstr_struct217]  ; Load string struct address
    mov rcx, rax
    call puts
    add rsp, 32
    ; Unknown return type - assuming integer
    ; IR call: exit (1 args)
    sub rsp, 32
    mov rax, 1
    mov rcx, rax
    call exit
    add rsp, 32
    ; Unknown return type - assuming integer
ir_nonnull_400:
    mov rax, 1
    mov r10, rax
    ; Load variable: pos
    movsxd rax, r13d      ; From register (signed int32)
    imul rax, r10
    mov [rbp - 3136], rax
    mov rax, [rbp - 3136]
    mov r10, rax
    ; Load variable: source
    mov rax, r15      ; From register
    add rax, r10
    mov [rbp - 3144], rax
    mov rax, [rbp - 3144]
    movzx rax, byte [rax]
    mov [rbp - 3152], rax
    ; IR call: is_digit (1 args)
    mov rax, [rbp - 3152]
    mov rcx, rax
    call is_digit
    ; Integer/pointer return value in rax
    ; 32-bit return value already in eax
    mov [rbp - 3160], rax
    mov rax, 0
    mov r10, rax
    mov rax, [rbp - 3160]
    cmp rax, r10
    je ir_while_end_398
    mov rax, 1
    mov r10, rax
    ; Load variable: pos
    movsxd rax, r13d      ; From register (signed int32)
    add rax, r10
    mov [rbp - 3176], rax
    mov rax, [rbp - 3176]
    ; Store to variable: pos
    mov r13d, eax       ; To register (int32/uint32)
    mov rax, 1
    mov r10, rax
    ; Load variable: col
    movsxd rax, r14d      ; From register (signed int32)
    add rax, r10
    mov [rbp - 3184], rax
    mov rax, [rbp - 3184]
    ; Store to variable: col
    mov r14d, eax       ; To register (int32/uint32)
    jmp ir_while_397
ir_while_end_398:
    jmp ir_if_end_385
ir_if_next_386:
ir_if_end_385:
    test r12, r12
    jnz ir_nonnull_402
ir_trap_null_401:
    ; IR call: puts (1 args)
    sub rsp, 32
    ; String literal (37 bytes)
    lea rax, [rel Lstr_struct219]  ; Load string struct address
    mov rcx, rax
    call puts
    add rsp, 32
    ; Unknown return type - assuming integer
    ; IR call: exit (1 args)
    sub rsp, 32
    mov rax, 1
    mov rcx, rax
    call exit
    add rsp, 32
    ; Unknown return type - assuming integer
ir_nonnull_402:
    mov rax, 0
    mov r10, rax
    ; Load variable: tok
    mov rax, r12      ; From register
    add rax, r10
    mov [rbp - 3192], rax
    ; Load variable: TOKEN_NUMBER
    movsxd rax, dword [rel TOKEN_NUMBER]  ; From global memory
    mov rcx, rax
    mov rax, [rbp - 3192]
    mov dword [rax], ecx
    test r12, r12
    jnz ir_nonnull_404
ir_trap_null_403:
    ; IR call: puts (1 args)
    sub rsp, 32
    ; String literal (37 bytes)
    lea rax, [rel Lstr_struct221]  ; Load string struct address
    mov rcx, rax
    call puts
    add rsp, 32
    ; Unknown return type - assuming integer
    ; IR call: exit (1 args)
    sub rsp, 32
    mov rax, 1
    mov rcx, rax
    call exit
    add rsp, 32
    ; Unknown return type - assuming integer
ir_nonnull_404:
    mov rax, 4
    mov r10, rax
    ; Load variable: tok
    mov rax, r12      ; From register
    add rax, r10
    mov [rbp - 3208], rax
    ; Load variable: start
    movsxd rax, dword [rbp - 80]  ; From stack [rbp - 80]
    mov rcx, rax
    mov rax, [rbp - 3208]
    mov dword [rax], ecx
    ; Load variable: start
    movsxd rax, dword [rbp - 80]  ; From stack [rbp - 80]
    mov r10, rax
    ; Load variable: pos
    movsxd rax, r13d      ; From register (signed int32)
    sub rax, r10
    mov [rbp - 3224], rax
    test r12, r12
    jnz ir_nonnull_406
ir_trap_null_405:
    ; IR call: puts (1 args)
    sub rsp, 32
    ; String literal (37 bytes)
    lea rax, [rel Lstr_struct223]  ; Load string struct address
    mov rcx, rax
    call puts
    add rsp, 32
    ; Unknown return type - assuming integer
    ; IR call: exit (1 args)
    sub rsp, 32
    mov rax, 1
    mov rcx, rax
    call exit
    add rsp, 32
    ; Unknown return type - assuming integer
ir_nonnull_406:
    mov rax, 8
    mov r10, rax
    ; Load variable: tok
    mov rax, r12      ; From register
    add rax, r10
    mov [rbp - 3232], rax
    mov rax, [rbp - 3224]
    mov rcx, rax
    mov rax, [rbp - 3232]
    mov dword [rax], ecx
    jmp ir_if_end_188
ir_if_next_372:
    test r12, r12
    jnz ir_nonnull_408
ir_trap_null_407:
    ; IR call: puts (1 args)
    sub rsp, 32
    ; String literal (37 bytes)
    lea rax, [rel Lstr_struct225]  ; Load string struct address
    mov rcx, rax
    call puts
    add rsp, 32
    ; Unknown return type - assuming integer
    ; IR call: exit (1 args)
    sub rsp, 32
    mov rax, 1
    mov rcx, rax
    call exit
    add rsp, 32
    ; Unknown return type - assuming integer
ir_nonnull_408:
    mov rax, 0
    mov r10, rax
    ; Load variable: tok
    mov rax, r12      ; From register
    add rax, r10
    mov [rbp - 3248], rax
    ; Load variable: TOKEN_ERROR
    movsxd rax, dword [rel TOKEN_ERROR]  ; From global memory
    mov rcx, rax
    mov rax, [rbp - 3248]
    mov dword [rax], ecx
    test r12, r12
    jnz ir_nonnull_410
ir_trap_null_409:
    ; IR call: puts (1 args)
    sub rsp, 32
    ; String literal (37 bytes)
    lea rax, [rel Lstr_struct227]  ; Load string struct address
    mov rcx, rax
    call puts
    add rsp, 32
    ; Unknown return type - assuming integer
    ; IR call: exit (1 args)
    sub rsp, 32
    mov rax, 1
    mov rcx, rax
    call exit
    add rsp, 32
    ; Unknown return type - assuming integer
ir_nonnull_410:
    mov rax, 8
    mov r10, rax
    ; Load variable: tok
    mov rax, r12      ; From register
    add rax, r10
    mov [rbp - 3264], rax
    mov rax, 1
    mov rcx, rax
    mov rax, [rbp - 3264]
    mov dword [rax], ecx
    mov rax, 1
    neg rax
    mov [rbp - 3280], rax
    mov rax, [rbp - 3280]
    mov [rbp - 3288], rax
    cmp qword [rbp - 3288], 0
    jnz ir_errdefer_end_412
ir_errdefer_ok_411:
ir_errdefer_end_412:
    mov rax, [rbp - 3280]
    jmp Llex_all_exit
ir_if_end_188:
    mov rax, 1
    mov r10, rax
    ; Load variable: count
    movsxd rax, dword [rbp - 72]  ; From stack [rbp - 72]
    add rax, r10
    mov [rbp - 3296], rax
    mov rax, [rbp - 3296]
    ; Store to variable: count
    mov dword [rbp - 72], eax  ; To stack [rbp - 72]
    jmp ir_while_128
ir_while_end_129:
    mov rax, 1
    neg rax
    mov [rbp - 3304], rax
    mov rax, [rbp - 3304]
    mov [rbp - 3312], rax
    cmp qword [rbp - 3312], 0
    jnz ir_errdefer_end_414
ir_errdefer_ok_413:
ir_errdefer_end_414:
    mov rax, [rbp - 3304]
    jmp Llex_all_exit
Llex_all_exit:
    mov rbx, [rbp - 64]
    mov r15, [rbp - 56]
    mov r14, [rbp - 48]
    mov r13, [rbp - 40]
    mov r12, [rbp - 32]
    ; Function epilogue
    mov rsp, rbp  ; Restore stack pointer
    pop rbp         ; Restore old base pointer
    ret               ; Return to caller

; Default program entry point
global mainCRTStartup
mainCRTStartup:
    sub rsp, 40      ; Shadow space + alignment
    ; Initialize garbage collector runtime
    lea rcx, [rsp + 40]
    extern gc_init
    call gc_init
    extern gc_shutdown
    call gc_shutdown
    mov rcx, 0       ; Default exit status
    extern ExitProcess
    call ExitProcess

; Data section for global variables
section .data
; Global variable: TOKEN_EOF (int32, 4 bytes)
TOKEN_EOF:
    dd 0

; Global variable: TOKEN_IDENT (int32, 4 bytes)
TOKEN_IDENT:
    dd 1

; Global variable: TOKEN_NUMBER (int32, 4 bytes)
TOKEN_NUMBER:
    dd 2

; Global variable: TOKEN_STRING (int32, 4 bytes)
TOKEN_STRING:
    dd 3

; Global variable: TOKEN_LET (int32, 4 bytes)
TOKEN_LET:
    dd 4

; Global variable: TOKEN_FN (int32, 4 bytes)
TOKEN_FN:
    dd 5

; Global variable: TOKEN_IF (int32, 4 bytes)
TOKEN_IF:
    dd 6

; Global variable: TOKEN_ELSE (int32, 4 bytes)
TOKEN_ELSE:
    dd 7

; Global variable: TOKEN_WHILE (int32, 4 bytes)
TOKEN_WHILE:
    dd 8

; Global variable: TOKEN_FOR (int32, 4 bytes)
TOKEN_FOR:
    dd 9

; Global variable: TOKEN_RETURN (int32, 4 bytes)
TOKEN_RETURN:
    dd 10

; Global variable: TOKEN_TRUE (int32, 4 bytes)
TOKEN_TRUE:
    dd 11

; Global variable: TOKEN_FALSE (int32, 4 bytes)
TOKEN_FALSE:
    dd 12

; Global variable: TOKEN_INT (int32, 4 bytes)
TOKEN_INT:
    dd 13

; Global variable: TOKEN_FLOAT (int32, 4 bytes)
TOKEN_FLOAT:
    dd 14

; Global variable: TOKEN_STRING_KW (int32, 4 bytes)
TOKEN_STRING_KW:
    dd 15

; Global variable: TOKEN_BOOL (int32, 4 bytes)
TOKEN_BOOL:
    dd 16

; Global variable: TOKEN_PLUS (int32, 4 bytes)
TOKEN_PLUS:
    dd 17

; Global variable: TOKEN_MINUS (int32, 4 bytes)
TOKEN_MINUS:
    dd 18

; Global variable: TOKEN_STAR (int32, 4 bytes)
TOKEN_STAR:
    dd 19

; Global variable: TOKEN_SLASH (int32, 4 bytes)
TOKEN_SLASH:
    dd 20

; Global variable: TOKEN_PERCENT (int32, 4 bytes)
TOKEN_PERCENT:
    dd 21

; Global variable: TOKEN_EQ (int32, 4 bytes)
TOKEN_EQ:
    dd 22

; Global variable: TOKEN_EQEQ (int32, 4 bytes)
TOKEN_EQEQ:
    dd 23

; Global variable: TOKEN_NE (int32, 4 bytes)
TOKEN_NE:
    dd 24

; Global variable: TOKEN_LT (int32, 4 bytes)
TOKEN_LT:
    dd 25

; Global variable: TOKEN_LE (int32, 4 bytes)
TOKEN_LE:
    dd 26

; Global variable: TOKEN_GT (int32, 4 bytes)
TOKEN_GT:
    dd 27

; Global variable: TOKEN_GE (int32, 4 bytes)
TOKEN_GE:
    dd 28

; Global variable: TOKEN_AND (int32, 4 bytes)
TOKEN_AND:
    dd 29

; Global variable: TOKEN_OR (int32, 4 bytes)
TOKEN_OR:
    dd 30

; Global variable: TOKEN_NOT (int32, 4 bytes)
TOKEN_NOT:
    dd 31

; Global variable: TOKEN_LPAREN (int32, 4 bytes)
TOKEN_LPAREN:
    dd 32

; Global variable: TOKEN_RPAREN (int32, 4 bytes)
TOKEN_RPAREN:
    dd 33

; Global variable: TOKEN_LBRACE (int32, 4 bytes)
TOKEN_LBRACE:
    dd 34

; Global variable: TOKEN_RBRACE (int32, 4 bytes)
TOKEN_RBRACE:
    dd 35

; Global variable: TOKEN_SEMI (int32, 4 bytes)
TOKEN_SEMI:
    dd 36

; Global variable: TOKEN_COLON (int32, 4 bytes)
TOKEN_COLON:
    dd 37

; Global variable: TOKEN_COMMA (int32, 4 bytes)
TOKEN_COMMA:
    dd 38

; Global variable: TOKEN_ARROW (int32, 4 bytes)
TOKEN_ARROW:
    dd 39

; Global variable: TOKEN_ERROR (int32, 4 bytes)
TOKEN_ERROR:
    dd 40

; Global variable: MAX_TOKENS (int32, 4 bytes)
MAX_TOKENS:
    dd 4096

Lstr_chars0:
    db "Fatal error: Null pointer dereference", 0
    align 8
Lstr_struct1:
    dq Lstr_chars0
    dq 37

Lstr_chars2:
    db "Fatal error: Null pointer dereference", 0
    align 8
Lstr_struct3:
    dq Lstr_chars2
    dq 37

Lstr_chars4:
    db "Fatal error: Null pointer dereference", 0
    align 8
Lstr_struct5:
    dq Lstr_chars4
    dq 37

Lstr_chars6:
    db "fn", 0
    align 8
Lstr_struct7:
    dq Lstr_chars6
    dq 2

Lstr_chars8:
    db "if", 0
    align 8
Lstr_struct9:
    dq Lstr_chars8
    dq 2

Lstr_chars10:
    db "let", 0
    align 8
Lstr_struct11:
    dq Lstr_chars10
    dq 3

Lstr_chars12:
    db "int", 0
    align 8
Lstr_struct13:
    dq Lstr_chars12
    dq 3

Lstr_chars14:
    db "for", 0
    align 8
Lstr_struct15:
    dq Lstr_chars14
    dq 3

Lstr_chars16:
    db "else", 0
    align 8
Lstr_struct17:
    dq Lstr_chars16
    dq 4

Lstr_chars18:
    db "true", 0
    align 8
Lstr_struct19:
    dq Lstr_chars18
    dq 4

Lstr_chars20:
    db "bool", 0
    align 8
Lstr_struct21:
    dq Lstr_chars20
    dq 4

Lstr_chars22:
    db "while", 0
    align 8
Lstr_struct23:
    dq Lstr_chars22
    dq 5

Lstr_chars24:
    db "false", 0
    align 8
Lstr_struct25:
    dq Lstr_chars24
    dq 5

Lstr_chars26:
    db "float", 0
    align 8
Lstr_struct27:
    dq Lstr_chars26
    dq 5

Lstr_chars28:
    db "return", 0
    align 8
Lstr_struct29:
    dq Lstr_chars28
    dq 6

Lstr_chars30:
    db "string", 0
    align 8
Lstr_struct31:
    dq Lstr_chars30
    dq 6

Lstr_chars32:
    db "Fatal error: Null pointer dereference", 0
    align 8
Lstr_struct33:
    dq Lstr_chars32
    dq 37

Lstr_chars34:
    db "Fatal error: Null pointer dereference", 0
    align 8
Lstr_struct35:
    dq Lstr_chars34
    dq 37

Lstr_chars36:
    db "Fatal error: Null pointer dereference", 0
    align 8
Lstr_struct37:
    dq Lstr_chars36
    dq 37

Lstr_chars38:
    db "Fatal error: Null pointer dereference", 0
    align 8
Lstr_struct39:
    dq Lstr_chars38
    dq 37

Lstr_chars40:
    db "Fatal error: Null pointer dereference", 0
    align 8
Lstr_struct41:
    dq Lstr_chars40
    dq 37

Lstr_chars42:
    db "Fatal error: Null pointer dereference", 0
    align 8
Lstr_struct43:
    dq Lstr_chars42
    dq 37

Lstr_chars44:
    db "Fatal error: Null pointer dereference", 0
    align 8
Lstr_struct45:
    dq Lstr_chars44
    dq 37

Lstr_chars46:
    db "Fatal error: Null pointer dereference", 0
    align 8
Lstr_struct47:
    dq Lstr_chars46
    dq 37

Lstr_chars48:
    db "Fatal error: Null pointer dereference", 0
    align 8
Lstr_struct49:
    dq Lstr_chars48
    dq 37

Lstr_chars50:
    db "Fatal error: Null pointer dereference", 0
    align 8
Lstr_struct51:
    dq Lstr_chars50
    dq 37

Lstr_chars52:
    db "Fatal error: Null pointer dereference", 0
    align 8
Lstr_struct53:
    dq Lstr_chars52
    dq 37

Lstr_chars54:
    db "Fatal error: Null pointer dereference", 0
    align 8
Lstr_struct55:
    dq Lstr_chars54
    dq 37

Lstr_chars56:
    db "Fatal error: Null pointer dereference", 0
    align 8
Lstr_struct57:
    dq Lstr_chars56
    dq 37

Lstr_chars58:
    db "Fatal error: Null pointer dereference", 0
    align 8
Lstr_struct59:
    dq Lstr_chars58
    dq 37

Lstr_chars60:
    db "Fatal error: Null pointer dereference", 0
    align 8
Lstr_struct61:
    dq Lstr_chars60
    dq 37

Lstr_chars62:
    db "Fatal error: Null pointer dereference", 0
    align 8
Lstr_struct63:
    dq Lstr_chars62
    dq 37

Lstr_chars64:
    db "Fatal error: Null pointer dereference", 0
    align 8
Lstr_struct65:
    dq Lstr_chars64
    dq 37

Lstr_chars66:
    db "Fatal error: Null pointer dereference", 0
    align 8
Lstr_struct67:
    dq Lstr_chars66
    dq 37

Lstr_chars68:
    db "Fatal error: Null pointer dereference", 0
    align 8
Lstr_struct69:
    dq Lstr_chars68
    dq 37

Lstr_chars70:
    db "Fatal error: Null pointer dereference", 0
    align 8
Lstr_struct71:
    dq Lstr_chars70
    dq 37

Lstr_chars72:
    db "Fatal error: Null pointer dereference", 0
    align 8
Lstr_struct73:
    dq Lstr_chars72
    dq 37

Lstr_chars74:
    db "Fatal error: Null pointer dereference", 0
    align 8
Lstr_struct75:
    dq Lstr_chars74
    dq 37

Lstr_chars76:
    db "Fatal error: Null pointer dereference", 0
    align 8
Lstr_struct77:
    dq Lstr_chars76
    dq 37

Lstr_chars78:
    db "Fatal error: Null pointer dereference", 0
    align 8
Lstr_struct79:
    dq Lstr_chars78
    dq 37

Lstr_chars80:
    db "Fatal error: Null pointer dereference", 0
    align 8
Lstr_struct81:
    dq Lstr_chars80
    dq 37

Lstr_chars82:
    db "Fatal error: Null pointer dereference", 0
    align 8
Lstr_struct83:
    dq Lstr_chars82
    dq 37

Lstr_chars84:
    db "Fatal error: Null pointer dereference", 0
    align 8
Lstr_struct85:
    dq Lstr_chars84
    dq 37

Lstr_chars86:
    db "Fatal error: Null pointer dereference", 0
    align 8
Lstr_struct87:
    dq Lstr_chars86
    dq 37

Lstr_chars88:
    db "Fatal error: Null pointer dereference", 0
    align 8
Lstr_struct89:
    dq Lstr_chars88
    dq 37

Lstr_chars90:
    db "Fatal error: Null pointer dereference", 0
    align 8
Lstr_struct91:
    dq Lstr_chars90
    dq 37

Lstr_chars92:
    db "Fatal error: Null pointer dereference", 0
    align 8
Lstr_struct93:
    dq Lstr_chars92
    dq 37

Lstr_chars94:
    db "Fatal error: Null pointer dereference", 0
    align 8
Lstr_struct95:
    dq Lstr_chars94
    dq 37

Lstr_chars96:
    db "Fatal error: Null pointer dereference", 0
    align 8
Lstr_struct97:
    dq Lstr_chars96
    dq 37

Lstr_chars98:
    db "Fatal error: Null pointer dereference", 0
    align 8
Lstr_struct99:
    dq Lstr_chars98
    dq 37

Lstr_chars100:
    db "Fatal error: Null pointer dereference", 0
    align 8
Lstr_struct101:
    dq Lstr_chars100
    dq 37

Lstr_chars102:
    db "Fatal error: Null pointer dereference", 0
    align 8
Lstr_struct103:
    dq Lstr_chars102
    dq 37

Lstr_chars104:
    db "Fatal error: Null pointer dereference", 0
    align 8
Lstr_struct105:
    dq Lstr_chars104
    dq 37

Lstr_chars106:
    db "Fatal error: Null pointer dereference", 0
    align 8
Lstr_struct107:
    dq Lstr_chars106
    dq 37

Lstr_chars108:
    db "Fatal error: Null pointer dereference", 0
    align 8
Lstr_struct109:
    dq Lstr_chars108
    dq 37

Lstr_chars110:
    db "Fatal error: Null pointer dereference", 0
    align 8
Lstr_struct111:
    dq Lstr_chars110
    dq 37

Lstr_chars112:
    db "Fatal error: Null pointer dereference", 0
    align 8
Lstr_struct113:
    dq Lstr_chars112
    dq 37

Lstr_chars114:
    db "Fatal error: Null pointer dereference", 0
    align 8
Lstr_struct115:
    dq Lstr_chars114
    dq 37

Lstr_chars116:
    db "Fatal error: Null pointer dereference", 0
    align 8
Lstr_struct117:
    dq Lstr_chars116
    dq 37

Lstr_chars118:
    db "Fatal error: Null pointer dereference", 0
    align 8
Lstr_struct119:
    dq Lstr_chars118
    dq 37

Lstr_chars120:
    db "Fatal error: Null pointer dereference", 0
    align 8
Lstr_struct121:
    dq Lstr_chars120
    dq 37

Lstr_chars122:
    db "Fatal error: Null pointer dereference", 0
    align 8
Lstr_struct123:
    dq Lstr_chars122
    dq 37

Lstr_chars124:
    db "Fatal error: Null pointer dereference", 0
    align 8
Lstr_struct125:
    dq Lstr_chars124
    dq 37

Lstr_chars126:
    db "Fatal error: Null pointer dereference", 0
    align 8
Lstr_struct127:
    dq Lstr_chars126
    dq 37

Lstr_chars128:
    db "Fatal error: Null pointer dereference", 0
    align 8
Lstr_struct129:
    dq Lstr_chars128
    dq 37

Lstr_chars130:
    db "Fatal error: Null pointer dereference", 0
    align 8
Lstr_struct131:
    dq Lstr_chars130
    dq 37

Lstr_chars132:
    db "Fatal error: Null pointer dereference", 0
    align 8
Lstr_struct133:
    dq Lstr_chars132
    dq 37

Lstr_chars134:
    db "Fatal error: Null pointer dereference", 0
    align 8
Lstr_struct135:
    dq Lstr_chars134
    dq 37

Lstr_chars136:
    db "Fatal error: Null pointer dereference", 0
    align 8
Lstr_struct137:
    dq Lstr_chars136
    dq 37

Lstr_chars138:
    db "Fatal error: Null pointer dereference", 0
    align 8
Lstr_struct139:
    dq Lstr_chars138
    dq 37

Lstr_chars140:
    db "Fatal error: Null pointer dereference", 0
    align 8
Lstr_struct141:
    dq Lstr_chars140
    dq 37

Lstr_chars142:
    db "Fatal error: Null pointer dereference", 0
    align 8
Lstr_struct143:
    dq Lstr_chars142
    dq 37

Lstr_chars144:
    db "Fatal error: Null pointer dereference", 0
    align 8
Lstr_struct145:
    dq Lstr_chars144
    dq 37

Lstr_chars146:
    db "Fatal error: Null pointer dereference", 0
    align 8
Lstr_struct147:
    dq Lstr_chars146
    dq 37

Lstr_chars148:
    db "Fatal error: Null pointer dereference", 0
    align 8
Lstr_struct149:
    dq Lstr_chars148
    dq 37

Lstr_chars150:
    db "Fatal error: Null pointer dereference", 0
    align 8
Lstr_struct151:
    dq Lstr_chars150
    dq 37

Lstr_chars152:
    db "Fatal error: Null pointer dereference", 0
    align 8
Lstr_struct153:
    dq Lstr_chars152
    dq 37

Lstr_chars154:
    db "Fatal error: Null pointer dereference", 0
    align 8
Lstr_struct155:
    dq Lstr_chars154
    dq 37

Lstr_chars156:
    db "Fatal error: Null pointer dereference", 0
    align 8
Lstr_struct157:
    dq Lstr_chars156
    dq 37

Lstr_chars158:
    db "Fatal error: Null pointer dereference", 0
    align 8
Lstr_struct159:
    dq Lstr_chars158
    dq 37

Lstr_chars160:
    db "Fatal error: Null pointer dereference", 0
    align 8
Lstr_struct161:
    dq Lstr_chars160
    dq 37

Lstr_chars162:
    db "Fatal error: Null pointer dereference", 0
    align 8
Lstr_struct163:
    dq Lstr_chars162
    dq 37

Lstr_chars164:
    db "Fatal error: Null pointer dereference", 0
    align 8
Lstr_struct165:
    dq Lstr_chars164
    dq 37

Lstr_chars166:
    db "Fatal error: Null pointer dereference", 0
    align 8
Lstr_struct167:
    dq Lstr_chars166
    dq 37

Lstr_chars168:
    db "Fatal error: Null pointer dereference", 0
    align 8
Lstr_struct169:
    dq Lstr_chars168
    dq 37

Lstr_chars170:
    db "Fatal error: Null pointer dereference", 0
    align 8
Lstr_struct171:
    dq Lstr_chars170
    dq 37

Lstr_chars172:
    db "Fatal error: Null pointer dereference", 0
    align 8
Lstr_struct173:
    dq Lstr_chars172
    dq 37

Lstr_chars174:
    db "Fatal error: Null pointer dereference", 0
    align 8
Lstr_struct175:
    dq Lstr_chars174
    dq 37

Lstr_chars176:
    db "Fatal error: Null pointer dereference", 0
    align 8
Lstr_struct177:
    dq Lstr_chars176
    dq 37

Lstr_chars178:
    db "Fatal error: Null pointer dereference", 0
    align 8
Lstr_struct179:
    dq Lstr_chars178
    dq 37

Lstr_chars180:
    db "Fatal error: Null pointer dereference", 0
    align 8
Lstr_struct181:
    dq Lstr_chars180
    dq 37

Lstr_chars182:
    db "Fatal error: Null pointer dereference", 0
    align 8
Lstr_struct183:
    dq Lstr_chars182
    dq 37

Lstr_chars184:
    db "Fatal error: Null pointer dereference", 0
    align 8
Lstr_struct185:
    dq Lstr_chars184
    dq 37

Lstr_chars186:
    db "Fatal error: Null pointer dereference", 0
    align 8
Lstr_struct187:
    dq Lstr_chars186
    dq 37

Lstr_chars188:
    db "Fatal error: Null pointer dereference", 0
    align 8
Lstr_struct189:
    dq Lstr_chars188
    dq 37

Lstr_chars190:
    db "Fatal error: Null pointer dereference", 0
    align 8
Lstr_struct191:
    dq Lstr_chars190
    dq 37

Lstr_chars192:
    db "Fatal error: Null pointer dereference", 0
    align 8
Lstr_struct193:
    dq Lstr_chars192
    dq 37

Lstr_chars194:
    db "Fatal error: Null pointer dereference", 0
    align 8
Lstr_struct195:
    dq Lstr_chars194
    dq 37

Lstr_chars196:
    db "Fatal error: Null pointer dereference", 0
    align 8
Lstr_struct197:
    dq Lstr_chars196
    dq 37

Lstr_chars198:
    db "Fatal error: Null pointer dereference", 0
    align 8
Lstr_struct199:
    dq Lstr_chars198
    dq 37

Lstr_chars200:
    db "Fatal error: Null pointer dereference", 0
    align 8
Lstr_struct201:
    dq Lstr_chars200
    dq 37

Lstr_chars202:
    db "Fatal error: Null pointer dereference", 0
    align 8
Lstr_struct203:
    dq Lstr_chars202
    dq 37

Lstr_chars204:
    db "Fatal error: Null pointer dereference", 0
    align 8
Lstr_struct205:
    dq Lstr_chars204
    dq 37

Lstr_chars206:
    db "Fatal error: Null pointer dereference", 0
    align 8
Lstr_struct207:
    dq Lstr_chars206
    dq 37

Lstr_chars208:
    db "Fatal error: Null pointer dereference", 0
    align 8
Lstr_struct209:
    dq Lstr_chars208
    dq 37

Lstr_chars210:
    db "Fatal error: Null pointer dereference", 0
    align 8
Lstr_struct211:
    dq Lstr_chars210
    dq 37

Lstr_chars212:
    db "Fatal error: Null pointer dereference", 0
    align 8
Lstr_struct213:
    dq Lstr_chars212
    dq 37

Lstr_chars214:
    db "Fatal error: Null pointer dereference", 0
    align 8
Lstr_struct215:
    dq Lstr_chars214
    dq 37

Lstr_chars216:
    db "Fatal error: Null pointer dereference", 0
    align 8
Lstr_struct217:
    dq Lstr_chars216
    dq 37

Lstr_chars218:
    db "Fatal error: Null pointer dereference", 0
    align 8
Lstr_struct219:
    dq Lstr_chars218
    dq 37

Lstr_chars220:
    db "Fatal error: Null pointer dereference", 0
    align 8
Lstr_struct221:
    dq Lstr_chars220
    dq 37

Lstr_chars222:
    db "Fatal error: Null pointer dereference", 0
    align 8
Lstr_struct223:
    dq Lstr_chars222
    dq 37

Lstr_chars224:
    db "Fatal error: Null pointer dereference", 0
    align 8
Lstr_struct225:
    dq Lstr_chars224
    dq 37

Lstr_chars226:
    db "Fatal error: Null pointer dereference", 0
    align 8
Lstr_struct227:
    dq Lstr_chars226
    dq 37

